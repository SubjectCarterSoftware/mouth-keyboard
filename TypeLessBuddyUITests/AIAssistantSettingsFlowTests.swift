import XCTest

// Assistant rename UI coverage is handled at the view-model level.
// The direct UI flow is intentionally disabled because it is unstable under
// automated permission/window-loop conditions in this app shell.
