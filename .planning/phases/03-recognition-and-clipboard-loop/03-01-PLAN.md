---
phase: 03-recognition-and-clipboard-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Speech2Test/Activation/RecordingState.swift
  - Speech2Test/Transcription/WhisperService.swift
  - Speech2Test/Transcription/TranscriptionResult.swift
  - Speech2Test/Audio/AudioBufferAccumulator.swift
  - Speech2Test/Clipboard/ClipboardService.swift
  - Speech2Test/Activation/SpacebarInterceptor.swift
  - Speech2Test/Persistence/ShellPreferences.swift
  - Speech2TestTests/WhisperServiceTests.swift
  - Speech2TestTests/ClipboardServiceTests.swift
  - Speech2TestTests/AudioBufferAccumulatorTests.swift
  - Speech2TestTests/SpacebarInterceptorTests.swift
  - Speech2TestTests/ShellPreferencesPhase3Tests.swift
  - Speech2Test.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - TRNS-01
  - TRNS-02
  - TRNS-06
  - SESS-01
  - CLIP-01
  - FEED-01
  - FEED-03

must_haves:
  truths:
    - "RecordingState enum has idle, recording, processing, success, and failure cases"
    - "WhisperService can load a model and transcribe Float32 audio samples on a background actor"
    - "AudioBufferAccumulator collects AVAudioPCMBuffer chunks and converts to 16kHz mono Float32"
    - "ClipboardService writes text to NSPasteboard and can simulate Cmd+V auto-paste"
    - "SpacebarInterceptor intercepts spacebar via CGEventTap only when active"
    - "ShellPreferences has autoPasteEnabled and indicatorVisible keys"
  artifacts:
    - path: "Speech2Test/Activation/RecordingState.swift"
      provides: "Expanded RecordingState enum with processing, success, failure cases and FailureReason"
      contains: "case processing"
    - path: "Speech2Test/Transcription/WhisperService.swift"
      provides: "Actor-isolated whisper.cpp wrapper"
      exports: ["WhisperService"]
    - path: "Speech2Test/Transcription/TranscriptionResult.swift"
      provides: "Result type for transcription outcomes"
      exports: ["TranscriptionResult"]
    - path: "Speech2Test/Audio/AudioBufferAccumulator.swift"
      provides: "Buffer accumulation and format conversion"
      exports: ["AudioBufferAccumulator"]
    - path: "Speech2Test/Clipboard/ClipboardService.swift"
      provides: "Clipboard write and auto-paste"
      exports: ["ClipboardService"]
    - path: "Speech2Test/Activation/SpacebarInterceptor.swift"
      provides: "CGEventTap spacebar interception"
      exports: ["SpacebarInterceptor"]
  key_links:
    - from: "Speech2Test/Transcription/WhisperService.swift"
      to: "whisper.cpp C API"
      via: "whisper_init_from_file_with_params, whisper_full"
      pattern: "whisper_full"
    - from: "Speech2Test/Audio/AudioBufferAccumulator.swift"
      to: "AVAudioConverter"
      via: "16kHz mono Float32 conversion"
      pattern: "AVAudioConverter"
    - from: "Speech2Test/Clipboard/ClipboardService.swift"
      to: "NSPasteboard"
      via: "setString forType .string"
      pattern: "NSPasteboard\\.general"
---

<objective>
Build all Phase 3 service contracts, data types, and standalone services needed by the recognition-and-clipboard loop.

Purpose: Establish the foundational types (RecordingState expansion, TranscriptionResult, FailureReason) and independent services (WhisperService, AudioBufferAccumulator, ClipboardService, SpacebarInterceptor) so that Plan 02 can wire them together without ambiguity. Also creates Wave 0 test scaffolds for all new services.

Output: 6 production source files (1 expanded, 5 new), 5 new test files, whisper.spm SPM dependency added to the Xcode project.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recognition-and-clipboard-loop/03-CONTEXT.md
@.planning/phases/03-recognition-and-clipboard-loop/03-RESEARCH.md
@.planning/phases/03-recognition-and-clipboard-loop/03-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Speech2Test/Activation/RecordingState.swift (CURRENT — will be expanded):
```swift
enum TapMode: String, CaseIterable, Hashable {
    case single
    case double
}

enum RecordingState: Equatable {
    case idle
    case recording
}
```

From Speech2Test/Audio/AudioCaptureService.swift (tap callback — buffer accumulator hooks here):
```swift
// Line 153: the tap callback currently only feeds levelMonitor
inputNode.installTap(onBus: 0, bufferSize: 4_096, format: nil) { [weak self] buffer, _ in
    self?.levelMonitor?.process(buffer: buffer)
}
```

From Speech2Test/Audio/AudioLevelMonitor.swift:
```swift
@MainActor
final class AudioLevelMonitor: ObservableObject {
    @Published private(set) var level: Float = 0.0
    nonisolated func process(buffer: AVAudioPCMBuffer)
    func reset()
}
```

From Speech2Test/Persistence/ShellPreferences.swift (will add new keys):
```swift
@MainActor
final class ShellPreferences: ObservableObject {
    enum Keys {
        static let suiteName = "com.elicarter.Speech2Test.shell"
        // ... existing keys
    }
    // New keys needed: autoPasteEnabled, indicatorVisible
}
```

From Speech2Test/Activation/ActivationStore.swift:
```swift
@MainActor
final class ActivationStore: ObservableObject {
    @Published private(set) var state: RecordingState = .idle
    func arm()
    func stop()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create type contracts and standalone services with test scaffolds</name>
  <files>
    Speech2Test/Activation/RecordingState.swift,
    Speech2Test/Transcription/TranscriptionResult.swift,
    Speech2Test/Transcription/WhisperService.swift,
    Speech2Test/Audio/AudioBufferAccumulator.swift,
    Speech2Test/Clipboard/ClipboardService.swift,
    Speech2Test/Activation/SpacebarInterceptor.swift,
    Speech2Test/Persistence/ShellPreferences.swift,
    Speech2TestTests/WhisperServiceTests.swift,
    Speech2TestTests/ClipboardServiceTests.swift,
    Speech2TestTests/AudioBufferAccumulatorTests.swift,
    Speech2TestTests/SpacebarInterceptorTests.swift,
    Speech2TestTests/ShellPreferencesPhase3Tests.swift
  </files>
  <behavior>
    RecordingState:
    - RecordingState has cases: idle, recording, processing, success(text: String), failure(reason: FailureReason)
    - FailureReason has cases: noSpeechDetected, modelError(String), silenceTimeout
    - RecordingState conforms to Equatable
    - RecordingState.isTerminal returns true for success and failure, false otherwise

    TranscriptionResult:
    - TranscriptionResult enum: success(String), failure(FailureReason) where FailureReason is RecordingState.FailureReason

    WhisperService (test with protocol mock — real whisper model not available in CI):
    - WhisperService is an actor
    - loadModel(at:) throws TranscriptionError.modelLoadFailed when path is invalid
    - transcribe(samples:) throws TranscriptionError.noModel when no model loaded
    - transcribe(samples:) throws TranscriptionError.noSpeechDetected when result is empty
    - Define TranscriptionError enum: modelLoadFailed, noModel, inferenceFailed, noSpeechDetected
    - Define WhisperTranscribing protocol for testability: func transcribe(samples: [Float]) async throws -> String

    AudioBufferAccumulator:
    - append(_:) stores buffer references
    - reset() clears all stored buffers
    - totalFrameCount returns sum of all buffer frameLengths
    - convertToWhisperFormat() returns [Float] of 16kHz mono samples
    - convertToWhisperFormat() throws on empty buffer list

    ClipboardService:
    - writeToClipboard(_:) writes text to NSPasteboard.general and returns Bool
    - After writeToClipboard, NSPasteboard.general.string(forType: .string) equals the written text
    - ClipboardService can be initialized with a custom NSPasteboard for testing
    - autoPaste() posts CGEvent Cmd+V (tested via protocol mock, not actual events)

    SpacebarInterceptor:
    - SpacebarInterceptor has isActive Bool property (default false)
    - When isActive is false, spacebar events are not consumed (passed through)
    - When isActive is true and keyCode == 49 (space), event is consumed and onSpacebarPressed fires
    - Tests use a protocol mock (SpacebarHandling) since CGEventTap requires Accessibility permission

    ShellPreferences (Phase 3 additions):
    - autoPasteEnabled defaults to true
    - indicatorVisible defaults to true
    - Both persist to UserDefaults on set
  </behavior>
  <action>
    **Step 1: Add whisper.spm SPM dependency.**
    The whisper.spm package must be added to the Xcode project. Add `https://github.com/ggerganov/whisper.spm` with dependency rule set to branch: master. This requires editing project.pbxproj to add the package reference and link the whisper library target. Per research, branch: master is required to avoid unsafe build flag errors.

    **Step 2: Expand RecordingState.swift.**
    Replace the current two-case enum with the expanded version:
    - Add `case processing`, `case success(text: String)`, `case failure(reason: FailureReason)`
    - Add nested `enum FailureReason: Equatable` with cases: `noSpeechDetected`, `modelError(String)`, `silenceTimeout`
    - Add computed property `var isTerminal: Bool` returning true for success/failure
    - Keep TapMode enum unchanged in the same file

    **Step 3: Create Speech2Test/Transcription/ directory and files.**

    TranscriptionResult.swift:
    - `enum TranscriptionResult` with `case success(String)` and `case failure(RecordingState.FailureReason)`

    WhisperService.swift:
    - Define `protocol WhisperTranscribing: Sendable` with `func transcribe(samples: [Float]) async throws -> String`
    - Create `actor WhisperService: WhisperTranscribing`
    - Private `context: OpaquePointer?` for whisper context
    - `func loadModel(at path: String) throws` — uses `whisper_context_default_params()` with `flash_attn = true`, calls `whisper_init_from_file_with_params`. Throws `TranscriptionError.modelLoadFailed` on nil.
    - `func transcribe(samples: [Float]) throws -> String` — uses `whisper_full_default_params(WHISPER_SAMPLING_GREEDY)`, sets `n_threads` to `max(1, min(8, ProcessInfo.processInfo.processorCount - 2))`, language "en", `no_context = true`. Concatenates segments via `whisper_full_n_segments` / `whisper_full_get_segment_text`. Trims whitespace. Throws `.noSpeechDetected` if empty, `.inferenceFailed` if whisper_full returns non-zero.
    - `deinit` calls `whisper_free(context)` if non-nil
    - `enum TranscriptionError: LocalizedError` with cases: `modelLoadFailed`, `noModel`, `inferenceFailed`, `noSpeechDetected`

    **Step 4: Create Speech2Test/Audio/AudioBufferAccumulator.swift.**
    - `final class AudioBufferAccumulator`
    - Private `var buffers: [AVAudioPCMBuffer] = []` and `var inputFormat: AVAudioFormat?`
    - `nonisolated func append(_ buffer: AVAudioPCMBuffer)` — stores buffer, captures format from first buffer. Note: append is called from the audio tap thread, so use a lock (NSLock or os_unfair_lock) to protect the buffers array.
    - `var totalFrameCount: AVAudioFrameCount` — sum of all buffer frameLengths
    - `func convertToWhisperFormat() throws -> [Float]` — creates output format (16kHz, mono, Float32 standard), uses AVAudioConverter. Concatenate all buffers into one combined buffer first, then convert. Return `Array(UnsafeBufferPointer(...))`. Throw if buffers is empty.
    - `func reset()` — clears buffers and inputFormat

    **Step 5: Create Speech2Test/Clipboard/ClipboardService.swift.**
    - `final class ClipboardService`
    - Init accepts optional `NSPasteboard` (defaults to `.general`) for testability
    - `func writeToClipboard(_ text: String) -> Bool` — clearContents + setString
    - `func autoPaste()` — 50ms async delay, then post CGEvent keyDown/keyUp for Cmd+V (vKeyCode 9). Use `CGEventSource(stateID: .combinedSessionState)`. Set `.maskCommand` flag on both events. Post to `.cgAnnotatedSessionEventTap`.

    **Step 6: Create Speech2Test/Activation/SpacebarInterceptor.swift.**
    - `protocol SpacebarHandling: AnyObject` with `var isActive: Bool`, `var onSpacebarPressed: (() -> Void)?`, `func start()`, `func stop()`
    - `final class SpacebarInterceptor: SpacebarHandling`
    - `var isActive: Bool = false`
    - `var onSpacebarPressed: (() -> Void)?`
    - Private `eventTap: CFMachPort?`
    - `func start()` — create CGEventTap with `.cgSessionEventTap`, `.headInsertEventTap`, `.defaultTap`, mask for keyDown events. Callback checks `isActive`, then checks keyCode == 49 (kVK_Space). If match: dispatch `onSpacebarPressed` to main queue, return nil (swallow). Otherwise pass through. Add run loop source to `.commonModes`. Use `Unmanaged.passUnretained(self).toOpaque()` for userInfo.
    - `func stop()` — disable tap, remove from run loop, set eventTap to nil

    **Step 7: Expand ShellPreferences.swift.**
    Add to Keys enum: `static let autoPasteEnabled = "autoPasteEnabled"`, `static let indicatorVisible = "indicatorVisible"`
    Add `@Published var autoPasteEnabled: Bool` with didSet persisting to defaults. Default true.
    Add `@Published var indicatorVisible: Bool` with didSet persisting to defaults. Default true.
    Update `init(userDefaults:)` to load both (default true if missing).
    Update `reset()` to reset both to true and remove from defaults.

    **Step 8: Create all test files.**
    All test files should be added to the Speech2TestTests target in project.pbxproj.

    WhisperServiceTests.swift:
    - Create a `MockWhisperTranscriber: WhisperTranscribing` that returns canned results or throws
    - Test that mock returns expected text
    - Test that mock throws noSpeechDetected for empty result
    - Test TranscriptionError cases exist and have descriptions

    AudioBufferAccumulatorTests.swift:
    - Test append increases totalFrameCount
    - Test reset clears buffers (totalFrameCount == 0)
    - Test convertToWhisperFormat throws on empty buffers
    - Test convertToWhisperFormat returns Float array with correct sample count ratio (within tolerance for resampling)

    ClipboardServiceTests.swift:
    - Create test with a dedicated NSPasteboard (NSPasteboard(name: .init("test")))
    - Test writeToClipboard returns true and pasteboard contains the text
    - Test writing new text replaces old text

    SpacebarInterceptorTests.swift:
    - Test that SpacebarInterceptor conforms to SpacebarHandling
    - Test isActive defaults to false
    - Create MockSpacebarHandler for protocol-based testing
    - Test mock fires onSpacebarPressed when active and space pressed

    ShellPreferencesPhase3Tests.swift:
    - Test autoPasteEnabled defaults to true on fresh UserDefaults
    - Test indicatorVisible defaults to true on fresh UserDefaults
    - Test setting autoPasteEnabled persists to UserDefaults
    - Test setting indicatorVisible persists to UserDefaults

    **Step 9: Ensure project builds.**
    Add all new files to the Xcode project (project.pbxproj). Create the Transcription/ and Clipboard/ directories. Verify the project compiles with `xcodebuild build`.
  </action>
  <verify>
    <automated>cd /Users/elicarter/Workspace/speech2test && xcodebuild test -project Speech2Test.xcodeproj -scheme Speech2Test -only-testing Speech2TestTests -destination 'platform=macOS' 2>&1 | tail -30</automated>
  </verify>
  <done>
    - RecordingState.swift has 5 cases (idle, recording, processing, success, failure) and FailureReason enum
    - WhisperService actor exists with loadModel/transcribe, WhisperTranscribing protocol defined
    - AudioBufferAccumulator accumulates buffers and converts to 16kHz mono Float32
    - ClipboardService writes to NSPasteboard and has autoPaste method
    - SpacebarInterceptor creates CGEventTap for spacebar with isActive guard
    - ShellPreferences has autoPasteEnabled (default true) and indicatorVisible (default true)
    - All 5 test files pass
    - whisper.spm SPM package added to project
  </done>
</task>

</tasks>

<verification>
- `xcodebuild build -project Speech2Test.xcodeproj -scheme Speech2Test -destination 'platform=macOS'` succeeds
- `xcodebuild test -project Speech2Test.xcodeproj -scheme Speech2Test -only-testing Speech2TestTests -destination 'platform=macOS'` passes all tests
- All 6 production files exist in their respective directories
- All 5 test files exist in Speech2TestTests/
</verification>

<success_criteria>
- All new types compile and existing code still builds (RecordingState expansion must not break ActivationStore/AppDelegate switch statements — those files are NOT modified in this plan; they will need updates in Plan 02)
- Tests pass for all new services
- whisper.spm package resolves and links
</success_criteria>

<output>
After completion, create `.planning/phases/03-recognition-and-clipboard-loop/03-01-SUMMARY.md`
</output>
