---
phase: 01-foundation-and-permissions
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Speech2Test/Permissions/MicrophonePermissionService.swift
  - Speech2Test/Permissions/KeyboardPermissionService.swift
  - Speech2Test/Readiness/ReadinessSnapshot.swift
  - Speech2Test/Readiness/ReadinessStore.swift
  - Speech2Test/Shell/StatusCardView.swift
  - Speech2Test/Shell/PermissionChecklistView.swift
  - Speech2Test/Shell/RecoveryActions.swift
  - Speech2TestTests/PermissionServiceTests.swift
  - Speech2TestTests/ReadinessStateTests.swift
  - Speech2TestUITests/PermissionRecoveryFlowTests.swift
autonomous: true
requirements:
  - CONF-01
  - CONF-02
user_setup: []
must_haves:
  truths:
    - "The app can tell the user whether Speech2Test is ready, needs setup, or is blocked before any recording attempt."
    - "The first-run and blocked states clearly explain microphone and keyboard-monitoring prerequisites and recovery steps."
    - "When a permission becomes available, the shell can transition to ready state with a brief confirmation instead of silent failure."
  artifacts:
    - path: "Speech2Test/Permissions/MicrophonePermissionService.swift"
      provides: "Microphone permission preflight and request handling"
    - path: "Speech2Test/Permissions/KeyboardPermissionService.swift"
      provides: "Keyboard-monitoring readiness abstraction for the Phase 1 shell"
    - path: "Speech2Test/Readiness/ReadinessSnapshot.swift"
      provides: "Capability-based ready / needs-setup / blocked derivation"
    - path: "Speech2Test/Shell/PermissionChecklistView.swift"
      provides: "Checklist-style first-run and blocked-state guidance UI"
    - path: "Speech2TestUITests/PermissionRecoveryFlowTests.swift"
      provides: "Smoke coverage for setup and recovery flows"
  key_links:
    - "Permission services feed ReadinessSnapshot / ReadinessStore rather than embedding platform checks in SwiftUI views"
    - "StatusCardView and PermissionChecklistView render readiness state from the shared readiness model"
    - "RecoveryActions.swift exposes a clear Fix setup path that is driven by the relevant permission state"
---

<objective>
Implement the readiness model, permission services, and setup/recovery UX for Phase 1.

Purpose: Make the shell genuinely useful by telling the user whether Speech2Test is blocked or ready before recording exists.
Output: Capability-based permission services, readiness state/store, status card and checklist UI, and automated readiness/permission tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-permissions/01-CONTEXT.md
@.planning/phases/01-foundation-and-permissions/01-RESEARCH.md
@.planning/phases/01-foundation-and-permissions/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement permission capability services and readiness-state derivation</name>
  <files>Speech2Test/Permissions/MicrophonePermissionService.swift, Speech2Test/Permissions/KeyboardPermissionService.swift, Speech2Test/Readiness/ReadinessSnapshot.swift, Speech2Test/Readiness/ReadinessStore.swift</files>
  <action>Create small permission services for microphone and keyboard-monitoring readiness, then aggregate them in a shared readiness model/store that can answer ready, needs-setup, or blocked before recording exists. Keep keyboard permissions capability-based so the shell does not overfit to a single future implementation path. Do not bury permission logic in view structs or wire in actual hotkey/recording behavior from later phases.</action>
  <verify>xcodebuild test -scheme Speech2Test -destination 'platform=macOS' -only-testing:Speech2TestTests/PermissionServiceTests -only-testing:Speech2TestTests/ReadinessStateTests</verify>
  <done>Permission state and readiness derivation are testable, shared, and independent from the UI layer.</done>
</task>

<task type="auto">
  <name>Task 2: Build the compact status card, checklist setup flow, and recovery actions</name>
  <files>Speech2Test/Shell/StatusCardView.swift, Speech2Test/Shell/PermissionChecklistView.swift, Speech2Test/Shell/RecoveryActions.swift, Speech2Test/Readiness/ReadinessStore.swift</files>
  <action>Render the locked Phase 1 UX in the shell: a compact top-of-menu status card, persistent blocked state, a checklist-style first-run/setup flow, and a strong Fix setup recovery action that can explain and route the user toward System Settings. Keep the icon treatment subtle, keep the menu quiet when ready, and add only a brief ready confirmation when the blocked state clears.</action>
  <verify>xcodebuild build -scheme Speech2Test -destination 'platform=macOS'</verify>
  <done>The app can visually communicate ready vs blocked state and provide clear setup/recovery guidance without turning the menu into a full app wizard.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage and manual verification hooks for permission flows</name>
  <files>Speech2TestTests/PermissionServiceTests.swift, Speech2TestTests/ReadinessStateTests.swift, Speech2TestUITests/PermissionRecoveryFlowTests.swift</files>
  <action>Add the unit tests and UI smoke coverage required by the validation contract for permission-state mapping, readiness transitions, and the blocked recovery flow. Keep the tests Phase-1 scoped: they should validate shell/readiness behavior and not pull in recording or hotkey execution logic.</action>
  <verify>xcodebuild test -scheme Speech2Test -destination 'platform=macOS' -only-testing:Speech2TestTests/PermissionServiceTests -only-testing:Speech2TestTests/ReadinessStateTests -only-testing:Speech2TestUITests/PermissionRecoveryFlowTests</verify>
  <done>Automated verification exists for permission-state mapping, readiness derivation, and the recovery path expected in Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild test -scheme Speech2Test -destination 'platform=macOS' -only-testing:Speech2TestTests/PermissionServiceTests -only-testing:Speech2TestTests/ReadinessStateTests -only-testing:Speech2TestUITests/PermissionRecoveryFlowTests` succeeds
- [ ] The shell reports ready / needs-setup / blocked before any recording exists
- [ ] Blocked states provide recovery guidance and a clear Fix setup action for microphone and keyboard-monitoring prerequisites
</verification>

<success_criteria>

- All tasks completed
- Phase 1 shell can determine and display readiness before recording is implemented
- First-run and blocked states clearly guide the user through microphone and keyboard permission recovery
- Automated tests cover readiness derivation and setup/recovery flow behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-permissions/01-02-SUMMARY.md`
</output>
