---
phase: 01-foundation-and-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Speech2Test.xcodeproj/project.pbxproj
  - Speech2Test/App/Speech2TestApp.swift
  - Speech2Test/App/AppDelegate.swift
  - Speech2Test/Info.plist
  - Speech2Test/Shell/StatusMenuView.swift
  - Speech2Test/Shell/SetupWindowView.swift
  - Speech2Test/Persistence/ShellPreferences.swift
  - Speech2TestTests/ReadinessStateTests.swift
  - Speech2TestUITests/MenuBarShellSmokeTests.swift
autonomous: true
requirements:
  - FEED-02
user_setup: []
must_haves:
  truths:
    - "The app launches as a menu-bar-first utility while another macOS app remains frontmost."
    - "The first launch shows the setup surface once, and later launches stay in the background utility shell."
    - "Basic shell preferences persist across relaunches."
  artifacts:
    - path: "Speech2Test/App/Speech2TestApp.swift"
      provides: "Native app entry and menu bar scene wiring"
    - path: "Speech2Test/Shell/StatusMenuView.swift"
      provides: "Primary menu bar control surface"
    - path: "Speech2Test/Shell/SetupWindowView.swift"
      provides: "First-launch setup surface scaffold"
    - path: "Speech2Test/Persistence/ShellPreferences.swift"
      provides: "Persisted first-launch and shell preference storage"
    - path: "Speech2TestUITests/MenuBarShellSmokeTests.swift"
      provides: "Smoke coverage for shell launch behavior"
  key_links:
    - "Speech2TestApp.swift presents StatusMenuView as the menu-bar-first shell"
    - "Speech2TestApp.swift opens SetupWindowView on first launch and suppresses it after preferences persist"
    - "ShellPreferences.swift is read by the app shell to determine first-launch and steady-state behavior"
---

<objective>
Build the native macOS utility shell and persistence foundation for Speech2Test.

Purpose: Establish the menu-bar-first app lifecycle and baseline persistence that every later phase depends on.
Output: A greenfield macOS app target with menu bar presence, first-launch setup window scaffold, and test scaffolding for shell behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-permissions/01-CONTEXT.md
@.planning/phases/01-foundation-and-permissions/01-RESEARCH.md
@.planning/phases/01-foundation-and-permissions/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the native menu bar app shell and agent-style app lifecycle</name>
  <files>Speech2Test.xcodeproj/project.pbxproj, Speech2Test/App/Speech2TestApp.swift, Speech2Test/App/AppDelegate.swift, Speech2Test/Info.plist</files>
  <action>Create the macOS app target and wire a menu-bar-first app entry that behaves like a utility rather than a Dock-forward app. Configure the shell so steady-state use is menu-bar only, with the project-level structure ready for later phases. Set the app metadata or activation behavior required for a utility shell, but avoid adding recording, hotkey, or audio logic in this phase.</action>
  <verify>xcodebuild build -scheme Speech2Test -destination 'platform=macOS'</verify>
  <done>The project builds, the app launches, and the shell is configured to behave as a menu-bar-first utility.</done>
</task>

<task type="auto">
  <name>Task 2: Add the first-launch setup surface and persistent shell preferences</name>
  <files>Speech2Test/Shell/StatusMenuView.swift, Speech2Test/Shell/SetupWindowView.swift, Speech2Test/Persistence/ShellPreferences.swift</files>
  <action>Implement the first-launch setup window scaffold and the persistent preference model that decides whether setup should appear. Keep the menu bar surface restrained, with room for a compact status card later, and persist only the Phase 1 shell concerns such as first-launch completion and any minimal shell visibility flags. Do not turn the menu into a wizard or mix readiness logic into persistence models.</action>
  <verify>xcodebuild build -scheme Speech2Test -destination 'platform=macOS'</verify>
  <done>The setup surface opens once on first launch, shell preferences persist, and the menu bar remains the primary ongoing app surface.</done>
</task>

<task type="auto">
  <name>Task 3: Install Wave 0 shell tests and smoke coverage</name>
  <files>Speech2TestTests/ReadinessStateTests.swift, Speech2TestUITests/MenuBarShellSmokeTests.swift, Speech2Test.xcodeproj/project.pbxproj</files>
  <action>Create the unit/UI test targets and baseline shell smoke tests required by the validation contract. Add at least one shell launch smoke test and placeholder readiness-state test scaffolding that later plans can extend, so the project does not leave Phase 1 without executable verification infrastructure.</action>
  <verify>xcodebuild test -scheme Speech2Test -destination 'platform=macOS' -only-testing:Speech2TestUITests/MenuBarShellSmokeTests</verify>
  <done>The repo has working native test targets and at least one automated shell smoke test tied into the project.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild build -scheme Speech2Test -destination 'platform=macOS'` succeeds
- [ ] `xcodebuild test -scheme Speech2Test -destination 'platform=macOS' -only-testing:Speech2TestUITests/MenuBarShellSmokeTests` succeeds
- [ ] Launching the app shows the setup surface once and keeps the menu bar as the long-lived shell
</verification>

<success_criteria>

- All tasks completed
- The app shell exists as a menu-bar-first utility with no recording logic mixed into it
- First-launch setup and shell persistence are wired for Phase 2 and later phases to build on
- Native test infrastructure exists and runs for shell smoke coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-permissions/01-01-SUMMARY.md`
</output>
