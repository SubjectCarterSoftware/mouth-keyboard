---
phase: 15-settings-ux-for-ai-assistant-name
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - Speech2Text/Activation/TriggerProfileStore.swift
  - Speech2Text/Persistence/ShellPreferences.swift
  - Speech2Text/Shell/SetupWindowView.swift
  - Speech2Text/Shell/AIAssistantSettingsView.swift
  - Speech2TextTests/ActivationStoreTests.swift
  - Speech2TextUITests/AIAssistantSettingsFlowTests.swift
  - Speech2Text.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "UI tests can launch against isolated trigger-profile state without mutating the developer's real persisted trigger store."
    - "Automated coverage proves the AI Assistant tile renders, the sheet opens, preset/custom changes are reflected, and calibration summary text is visible through stable accessibility identifiers."
    - "ActivationStore regression tests prove updated preset/custom/calibrated aliases are consumed on the very next finalize path without requiring app restart."
    - "Final manual verification confirms real microphone/transcription flow respects trigger-name changes and calibration reruns replace prior aliases rather than merging them."
    - "Any polish from verification keeps the settings surface restrained: one tile, one change button, subdued status language, no warning-heavy default state."
  artifacts:
    - path: "Speech2Text/Activation/TriggerProfileStore.swift"
      provides: "Isolated trigger-profile persistence path or seed override support for UI testing"
      contains: "defaultStoreURL|loadSynchronously"
    - path: "Speech2Text/Persistence/ShellPreferences.swift"
      provides: "Launch-argument/test bootstrap for seeded trigger profile state"
      contains: "makeShared"
    - path: "Speech2TextUITests/AIAssistantSettingsFlowTests.swift"
      provides: "UI smoke coverage for tile visibility, sheet flow, preset/custom updates, and alias summary"
      contains: "test"
    - path: "Speech2TextTests/ActivationStoreTests.swift"
      provides: "No-restart runtime regressions after trigger-name and calibration updates"
      contains: "test_finalize|test_trigger"
  key_links:
    - from: "Speech2Text/Persistence/ShellPreferences.swift"
      to: "Speech2Text/Activation/TriggerProfileStore.swift"
      via: "UI-test launch arguments seed isolated trigger-profile state before the settings window renders"
      pattern: "ui-testing|trigger"
    - from: "Speech2TextUITests/AIAssistantSettingsFlowTests.swift"
      to: "Speech2Text/Shell/AIAssistantSettingsView.swift"
      via: "Accessibility identifiers exercise tile, sheet, preset buttons, custom save, and calibration summary states"
      pattern: "aiAssistant|assistantSettings"
    - from: "Speech2Text/Activation/ActivationStore.swift"
      to: "Speech2Text/Persistence/ShellPreferences.swift"
      via: "Finalize-time parsing consumes active trigger aliases immediately after settings mutations"
      pattern: "activeTriggerProfile|finalizeSession"
---

<objective>
Add the end-to-end verification harness for Phase 15, then run the final human sign-off loop so trigger-name configuration is proven in the live app without restart-only behavior or test pollution of real user state.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-settings-ux-for-ai-assistant-name/15-CONTEXT.md
@.planning/phases/15-settings-ux-for-ai-assistant-name/15-01-PLAN.md
@Speech2Text/Activation/TriggerProfileStore.swift
@Speech2Text/Persistence/ShellPreferences.swift
@Speech2Text/Activation/ActivationStore.swift
@Speech2Text/Shell/SetupWindowView.swift
@Speech2Text/Shell/AIAssistantSettingsView.swift
@Speech2TextUITests/MenuBarShellSmokeTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED end-to-end tests and isolated trigger-profile test hooks</name>
  <files>
    Speech2Text/Activation/TriggerProfileStore.swift,
    Speech2Text/Persistence/ShellPreferences.swift,
    Speech2TextTests/ActivationStoreTests.swift,
    Speech2TextUITests/AIAssistantSettingsFlowTests.swift,
    Speech2Text.xcodeproj/project.pbxproj
  </files>
  <action>
    Add the automated coverage and test bootstrap Phase 15 needs before manual sign-off.

    Implementation requirements:
    - Introduce a UI-testing-safe trigger-profile seed path so UI tests can start with default, alternate preset, or custom/calibrated assistant state without touching the real Application Support trigger store.
    - Add `ActivationStoreTests` coverage proving `setTriggerPreset`, `setCustomTrigger`, and `applyCalibrationAliases` affect the next finalize-time trigger parsing path immediately.
    - Add `AIAssistantSettingsFlowTests` covering:
      - AI Assistant tile appears in the setup window
      - `Change Assistant Name` opens the sheet
      - preset selection updates visible state
      - custom-name save updates visible state
      - calibration summary/alias state renders when seeded profile contains calibrated aliases
    - Reuse the accessibility identifiers from Plan 15-01 rather than brittle text-only queries wherever possible.
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/ActivationStoreTests -only-testing Speech2TextUITests/AIAssistantSettingsFlowTests -destination 'platform=macOS'</verify>
  <done>
    - deterministic UI/integration coverage exists for the Phase 15 flow
    - UI tests no longer depend on or mutate real user trigger-profile storage
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply verification-driven UX polish and accessibility adjustments until the automated gate passes</name>
  <files>
    Speech2Text/Shell/SetupWindowView.swift,
    Speech2Text/Shell/AIAssistantSettingsView.swift,
    Speech2Text/Persistence/ShellPreferences.swift,
    Speech2Text/Activation/TriggerProfileStore.swift,
    Speech2TextTests/ActivationStoreTests.swift,
    Speech2TextUITests/AIAssistantSettingsFlowTests.swift
  </files>
  <action>
    Fix whatever the new automated gate exposes while staying within Phase 15 scope.

    Focus areas:
    - accessibility identifiers and UI-test stability for tile, sheet, preset rows, custom save, calibration step text, and alias summary
    - quiet status copy and summary formatting so the tile stays scannable instead of noisy
    - repeated open/close, preset-to-custom transitions, and recalibration replacement behavior
    - preserving the Phase 15 boundary: no routing/parser rule changes beyond validating that runtime consumers see the updated trigger profile
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/ActivationStoreTests -only-testing Speech2TextUITests/AIAssistantSettingsFlowTests -destination 'platform=macOS'</verify>
  <done>
    - targeted automated gate is green
    - UX polish stays within the planned AI Assistant settings surface
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification — trigger-name settings flow and no-restart runtime behavior</name>
  <what-built>
    Complete Phase 15 AI Assistant settings surface:
    - dedicated AI Assistant tile in the setup window
    - single-sheet assistant name flow for preset/custom changes
    - guided three-sample calibration flow with retry + skip behavior
    - automated test harness for seeded trigger profiles and no-restart finalize-time alias updates
  </what-built>
  <how-to-verify>
    Launch the built app and open the setup/settings window, then verify the full flow:

    **SETT-01: Dedicated AI Assistant tile**
    1. Confirm the setup window shows an `AI Assistant` tile/section separate from Conversion Modes.
    2. Confirm the tile shows the active assistant name as the primary value.
    3. Confirm the secondary line is quiet and indicates whether the current name is default/preset/custom and whether aliases are calibrated.

    **SETT-02: Preset and custom name changes**
    4. Click `Change Assistant Name` and confirm a sheet opens in-place, not a second window.
    5. Select `Atlas` or `Gaia` and confirm the selection becomes active immediately without a save/confirm step.
    6. Close the sheet and confirm the tile reflects the new active name.
    7. Reopen the sheet, enter a custom name such as `Helios`, save it, and confirm the tile reflects the custom profile.

    **SETT-03: Calibration flow**
    8. From the same sheet, start calibration and confirm the flow expects three accepted samples.
    9. Intentionally provide one unusable/noisy attempt and confirm the UI requests a retry instead of counting it.
    10. Complete three valid samples and confirm a concise alias summary appears.
    11. Re-run calibration with a meaningfully different alias set and confirm the summary is replaced rather than merged with the old one.

    **No-restart runtime verification**
    12. Without restarting the app, trigger a fresh recording session using the newly selected preset/custom name and confirm post-trigger parsing works.
    13. Confirm the prior trigger name no longer activates parsing when it should not.
    14. If calibration added a new alias variant, confirm that alias activates parsing on the next session without relaunch.
  </how-to-verify>
  <action>Observe live behavior, including real microphone/transcription flow. Fix any bugs found before approving.</action>
  <verify>
    <automated>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/ActivationStoreTests -only-testing Speech2TextUITests/AIAssistantSettingsFlowTests -destination 'platform=macOS'</automated>
    <automated>xcodebuild build -scheme Speech2Text -destination 'platform=macOS' 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED"</automated>
  </verify>
  <done>All targeted automated tests pass, the app builds, and the live trigger-name flow behaves correctly without restart.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any failures found</resume-signal>
</task>

</tasks>

<verification>
Targeted ActivationStore and UI tests are green before manual sign-off.
Manual verification confirms trigger-name changes and calibration aliases affect subsequent sessions without restart.
</verification>

<success_criteria>
- Dedicated AI Assistant tile is present and low-distraction in the setup window
- Preset and custom name changes are usable through one sheet flow
- Calibration entry point remains in the same sheet and persists replacement aliases
- Runtime trigger parsing reflects changed names/aliases on the next session with no app restart
</success_criteria>

<output>
After completion, create `.planning/phases/15-settings-ux-for-ai-assistant-name/15-02-SUMMARY.md`
</output>
