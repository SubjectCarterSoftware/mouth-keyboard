---
phase: 02-activation-and-capture
task: 3
total_tasks: 3
status: paused
last_updated: 2026-03-06T04:00:00.000Z
---

<current_state>
Plan 02-03 Tasks 1 and 2 are committed. Task 3 (human verification) is in progress but blocked by an AVAudioEngine crash. The hotkey, pill UI, and AppDelegate wiring are working — the pill appears on double-tap — but audio capture crashes the app on start.
</current_state>

<completed_work>

- Plan 02-01: Done.
- Plan 02-02: Done.
- Plan 02-03 Task 1: Done (commit 459d007). RecordingPillView and RecordingPillPanel built and in Xcode project.
- Plan 02-03 Task 2: Done (commit 2ec8a27). AppDelegate wired: hotkey → audio → pill → icon → sound.
- Fix: NSMicrophoneUsageDescription added to Info.plist (commit 59b5fc0).
- Fix: arm() readiness gate changed to permissions-only (commit efe9b9c).
- Fix: CGEventTap replaced with KeyboardShortcuts Carbon hot key API — no Accessibility permission needed (commit 67a3525). This was the key fix: CGEventTap required Accessibility which was invalidated on every Xcode rebuild.
- Fix: Corrupted MACOSX_DEPLOYMENT_TARGET “14.0z “ repaired (commit 8cda968).
- Fix: Eager prepareAudioCaptureIfPossible removed from AppDelegate (commit 1716988).
- Fix: engine.prepare() called inside start() (commit 41fe384).
</completed_work>

<remaining_work>

- Fix AVAudioEngine crash: `engine.prepare()` throws an ObjC NSException (`inputNode != nullptr`) that Swift do/catch cannot intercept. Mic permission IS authorized at TCC level, but the engine still can't create an inputNode. Need to create an ObjC bridging header + exception catcher wrapper, OR find why inputNode is nil despite mic permission being granted.
- After audio crash is fixed: complete the 9-step human verification checklist.
- Write 02-03-SUMMARY.md and close the phase.
</remaining_work>

<decisions_made>

- Replaced CGEventTap with KeyboardShortcuts Carbon hot key API (RegisterEventHotKey). No Accessibility or Input Monitoring permission required. Double-tap state machine unchanged.
- KeyboardPermissionService always returns authorized (Carbon hot keys need no OS permission gate).
- Removed eager audio engine prewarm from AppDelegate — audio prepares on-demand in start().
- Audio capture still on AVAudioEngine.inputNode only and never routes to output nodes.
</decisions_made>

<blockers>
- AVAudioEngine.prepare() throws an ObjC NSException when inputNode is nil. This happens even though AVCaptureDevice.authorizationStatus(for: .audio) == .authorized. The crash prevents recording from starting. Need an ObjC exception catcher (bridging header) or a way to detect that inputNode will be nil before calling prepare().
- The “AddInstanceForFactory: No factory registered” CoreAudio log and the nil inputNode may indicate the audio hardware daemon is in a bad state, or that something else is needed beyond TCC mic permission.
</blockers>

<context>
The hotkey and pill UI are fully working. The only remaining blocker is the AVAudioEngine crash when starting audio capture. Once that's fixed, the full activation loop can be verified end-to-end, the summary written, and Phase 2 closed.
</context>

<next_action>
Create an ObjC bridging header with an NSException catcher so `engine.prepare()` failures are caught as Swift errors instead of crashing. Alternatively, investigate why AVAudioEngine has no inputNode despite mic permission being granted — may need to create the engine lazily or check CoreAudio device availability before touching the engine.
</next_action>
