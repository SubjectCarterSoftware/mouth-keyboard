---
phase: 02-activation-and-capture
task: 1
total_tasks: 3
status: paused
last_updated: 2026-03-06T02:52:21.750Z
---

<current_state>
Phase 2 execution is paused after completing plans 02-01 and 02-02. Plan 02-03 was started, but the executor interrupted before producing any 02-03 commit or summary. The next real work item is Task 1 of 02-03: build RecordingPillView and RecordingPillPanel, then continue into AppDelegate integration and the human verification checkpoint.
</current_state>

<completed_work>

- Plan 02-01: Done. Hotkey configuration, tap-mode persistence, ActivationStore, HotkeyService, and setup-window activation controls were implemented and verified.
- Plan 02-02: Done. AudioCaptureService, AudioDeviceService, AudioLevelMonitor, microphone picker UI, and launch-time audio prewarm were implemented and verified.
- Plan 02-03: Not completed. A first execution attempt created uncommitted pill-related files in the working tree but did not reach a checkpoint, produce commits, or write 02-03-SUMMARY.md.
</completed_work>

<remaining_work>

- Task 1: Finish `RecordingPillView.swift` and `RecordingPillPanel.swift`, then make sure they are added to the Xcode project cleanly.
- Task 2: Wire AppDelegate and activation flow so hotkey activation starts audio, shows the pill, changes the menu bar icon, and plays the activation sound.
- Task 3: Run the human verification checklist from 02-03-PLAN.md and capture the outcome. If it passes, write 02-03-SUMMARY.md and close the phase.
</remaining_work>

<decisions_made>

- Use `KeyboardShortcuts` only for recorder UI and persisted shortcut storage; keep actual interception in `CGEventTap` for single/double-tap control.
- Keep audio capture on `AVAudioEngine.inputNode` only and never route microphone input to output nodes.
- Treat “System Default” as a setup-window sentinel while `AudioDeviceService` enumerates only real CoreAudio devices.
- Skip launch-time audio prewarm during `-ui-testing` so UI permission flows stay deterministic.
</decisions_made>

<blockers>
- The first 02-03 executor attempt interrupted unexpectedly. There is no summary or commit history for 02-03, so resume from the working tree rather than from a completed checkpoint.
- Uncommitted Phase 2 UI work is present in the tree, including `Speech2Test/Shell/RecordingPillView.swift`, `Speech2Test/Shell/RecordingPillPanel.swift`, and a modified `Speech2Test.xcodeproj/project.pbxproj`. Review those before adding new edits.
</blockers>

<context>
The project is in a good state through the audio layer. The remaining work is integration-heavy rather than architecture-heavy: wire the existing activation and audio services into visible app behavior, then validate the full activation loop manually. The main risk is preserving the partially written 02-03 UI files without trampling them, then making AppDelegate coordination deterministic enough for the checkpoint verification.
</context>

<next_action>
Start with: inspect `Speech2Test/Shell/RecordingPillView.swift`, `Speech2Test/Shell/RecordingPillPanel.swift`, and `Speech2Test.xcodeproj/project.pbxproj` to see what the interrupted 02-03 attempt already changed, then continue Task 1 of `02-03-PLAN.md`.
</next_action>
