---
phase: 02-activation-and-capture
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - Speech2Test/Shell/RecordingPillView.swift
  - Speech2Test/Shell/RecordingPillPanel.swift
  - Speech2Test/App/AppDelegate.swift
  - Speech2Test/Activation/ActivationStore.swift
  - Speech2Test/Activation/HotkeyService.swift
  - Resources/activation.aiff
autonomous: false
requirements:
  - ACTV-04
  - AUDI-01
  - AUDI-02
  - AUDI-03

must_haves:
  truths:
    - "Pressing the configured hotkey (default Cmd+Shift+Z) starts recording immediately — pill appears, icon changes"
    - "The floating pill appears at the bottom center of the screen above all windows including full-screen apps"
    - "The pill shows a mic icon and a live waveform animation reflecting actual microphone input levels"
    - "The pill appears on all Spaces and desktops, not just the active Space"
    - "A brief activation sound plays when recording starts (unless disabled in settings)"
    - "The menu bar icon changes to a recording variant when recording is active"
    - "Recording begins within 50ms of hotkey activation so the user can speak immediately"
  artifacts:
    - path: "Speech2Test/Shell/RecordingPillView.swift"
      provides: "SwiftUI pill UI with mic icon and waveform bars driven by AudioLevelMonitor.level"
      exports: ["RecordingPillView"]
    - path: "Speech2Test/Shell/RecordingPillPanel.swift"
      provides: "NSPanel subclass: floating level, canJoinAllSpaces + fullScreenAuxiliary, non-activating"
      exports: ["RecordingPillPanel"]
    - path: "Speech2Test/App/AppDelegate.swift"
      provides: "Wires ActivationStore state changes to: pill show/hide, menu bar icon, audio start/stop, activation sound"
      contains: "pillPanel"
  key_links:
    - from: "Speech2Test/App/AppDelegate.swift"
      to: "Speech2Test/Activation/ActivationStore.swift"
      via: "observation of activationStore.$state driving pill visibility and audio start/stop"
      pattern: "activationStore\\.\\$state"
    - from: "Speech2Test/App/AppDelegate.swift"
      to: "Speech2Test/Audio/AudioCaptureService.swift"
      via: "audioCapture.start(levelMonitor:) called when state transitions to .recording"
      pattern: "audioCapture\\.start"
    - from: "Speech2Test/Shell/RecordingPillView.swift"
      to: "Speech2Test/Audio/AudioLevelMonitor.swift"
      via: "@ObservedObject levelMonitor — reads levelMonitor.level for waveform animation"
      pattern: "levelMonitor\\.level"
---

<objective>
Wire all Phase 2 components into a working end-to-end activation loop: hotkey fires, audio starts, pill appears with live waveform, menu bar icon changes, activation sound plays.

Purpose: This plan completes Phase 2 by connecting the state machine (Plan 01), audio stack (Plan 02), and AppDelegate into a single coherent flow that a user can trigger and observe. The pill panel is created once and shown/hidden reactively.

Output: Floating pill indicator visible on all Spaces; HotkeyService and ActivationStore started at launch; audio capture wired to state transitions; activation sound playback.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-activation-and-capture/02-CONTEXT.md
@.planning/phases/02-activation-and-capture/02-RESEARCH.md
@.planning/phases/02-activation-and-capture/02-01-SUMMARY.md
@.planning/phases/02-activation-and-capture/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plans 01 and 02 -->

From Speech2Test/Activation/ActivationStore.swift (Plan 01):
```swift
@MainActor final class ActivationStore: ObservableObject {
    static let shared: ActivationStore
    @Published private(set) var state: RecordingState  // .idle | .recording
    func arm()
    func stop()
}
```

From Speech2Test/Activation/HotkeyService.swift (Plan 01):
```swift
final class HotkeyService {
    var onArm: () -> Void   // set to { ActivationStore.shared.arm() } in production
    func start()            // installs CGEventTap
    func stop()
}
```

From Speech2Test/Audio/AudioCaptureService.swift (Plan 02):
```swift
final class AudioCaptureService {
    func prepare() throws
    func start(levelMonitor: AudioLevelMonitor) throws
    func stop()
}
```

From Speech2Test/Audio/AudioDeviceService.swift (Plan 02):
```swift
@MainActor final class AudioDeviceService: ObservableObject {
    static let shared: AudioDeviceService
    @Published private(set) var availableDevices: [AudioInputDevice]
    func setInputDevice(_ device: AudioInputDevice?, on engine: AVAudioEngine) throws
    func registerDisconnectListener(for deviceUID: String, onDisconnect: @escaping () -> Void)
}
```

From Speech2Test/Audio/AudioLevelMonitor.swift (Plan 02):
```swift
@MainActor final class AudioLevelMonitor: ObservableObject {
    @Published private(set) var level: Float  // 0.0–1.0
}
```

RecordingPillPanel pattern (from research Pattern 5):
```swift
// NSPanel subclass
// styleMask: [.nonactivatingPanel, .hudWindow, .utilityWindow]
// level = .floating
// collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
// isOpaque = false, backgroundColor = .clear
// canBecomeKey: false, canBecomeMain: false
// Content: NSHostingView<RecordingPillView>
// Position: bottom center of NSScreen.main, 40pt above dock
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RecordingPillView and RecordingPillPanel</name>
  <files>
    Speech2Test/Shell/RecordingPillView.swift,
    Speech2Test/Shell/RecordingPillPanel.swift
  </files>
  <action>
    1. Create Speech2Test/Shell/RecordingPillView.swift:
       - SwiftUI View with @ObservedObject var levelMonitor: AudioLevelMonitor
       - Visual design (Claude's discretion per CONTEXT.md):
         - Pill shape: RoundedRectangle(cornerRadius: 22) filled with dark translucent material (.ultraThinMaterial or .regularMaterial with dark colorScheme)
         - Size: approximately 160 × 44pt
         - Content (HStack, horizontally centered, spaced): mic.fill SF Symbol + waveform bars
         - Waveform: 5 bars (Rectangle shapes), heights driven by levelMonitor.level
           - Bar heights: multiply level by random per-bar scale factors for natural variation
           - Animate with .animation(.easeInOut(duration: 0.1), value: levelMonitor.level)
           - Min bar height: 4pt, max: 28pt, width: 3pt, spacing: 3pt
           - Bar color: white opacity 0.9
         - Mic icon: white, size 14pt, leading of bars
         - No interactive controls — read-only in Phase 2 (per CONTEXT.md)
       - Use .preferredColorScheme(.dark) to force dark appearance regardless of system setting
       - No background on the View itself — panel provides the visual container

    2. Create Speech2Test/Shell/RecordingPillPanel.swift:
       - NSPanel subclass per research Pattern 5:
         - init() creates the panel with correct style mask, level, collectionBehavior
         - styleMask: [.nonactivatingPanel, .hudWindow, .utilityWindow]
         - level = .floating
         - collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
         - isOpaque = false
         - backgroundColor = .clear
         - isMovableByWindowBackground = false
         - hasShadow = true
         - titleVisibility = .hidden
         - titlebarAppearsTransparent = true
         - canBecomeKey returns false
         - canBecomeMain returns false
       - Accepts levelMonitor: AudioLevelMonitor in init — wires to RecordingPillView
       - contentView = NSHostingView(rootView: RecordingPillView(levelMonitor: levelMonitor))
       - Position: bottom center of NSScreen.main, 40pt above screen origin (above Dock)
       - Width: 160, height: 44
       - func updatePosition() — called on init and when screen layout changes
  </action>
  <verify>
    The build succeeds with no new errors:
    xcodebuild build -scheme Speech2Test -destination 'platform=macOS' 2>&1 | tail -20
  </verify>
  <done>
    - RecordingPillView.swift compiles with waveform animation driven by AudioLevelMonitor.level.
    - RecordingPillPanel.swift compiles as NSPanel subclass with correct collection behavior.
    - Build is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: AppDelegate wiring — activation loop, audio start/stop, icon, sound</name>
  <files>
    Speech2Test/App/AppDelegate.swift,
    Speech2Test/Activation/ActivationStore.swift,
    Speech2Test/Activation/HotkeyService.swift,
    Resources/activation.aiff
  </files>
  <action>
    1. Bundle an activation sound:
       - Create or source a short (~200ms) subtle tone. Options:
         a. Use a macOS system sound that already exists in /System/Library/Sounds/ (e.g., "Tink.aiff") and copy to the app bundle as Resources/activation.aiff.
         b. Generate a simple sine wave .aiff programmatically in a setup script (if comfortable).
         c. Preferred: Use AudioServicesPlaySystemSound(1057) — the "keyboard click" system sound ID — as a fallback that requires no bundled file. This avoids file management entirely.
         Decision: Use system sound ID 1057 (keyboard click) for the activation sound in v1. No bundled audio file needed. If ShellPreferences.activationSoundEnabled == true, call AudioServicesPlaySystemSound(1057) in arm(). Mark Resources/activation.aiff as NOT NEEDED — use system sound ID approach instead.
       - Update ActivationStore.arm() to call the sound if enabled (inject sound player for testability).

    2. Add ActivationSoundPlayer to Speech2Test/Activation/ActivationStore.swift (or a separate small file):
       ```swift
       // Inline in ActivationStore or small struct
       struct ActivationSoundPlayer {
           // Uses system sound 1057 — no bundled file needed
           func play() {
               AudioServicesPlaySystemSound(1057)
           }
       }
       ```
       - ActivationStore.arm() calls soundPlayer.play() when preferences.activationSoundEnabled == true
       - soundPlayer is injectable (var soundPlayer: ActivationSoundPlayer = .init()) for testing

    3. Extend Speech2Test/App/AppDelegate.swift:

       New stored properties:
       - private let activationStore = ActivationStore.shared
       - private let hotkeyService = HotkeyService(...)  // configure onArm
       - private let audioCapture = AudioCaptureService()
       - private let audioDevice = AudioDeviceService.shared
       - private let levelMonitor = AudioLevelMonitor()
       - private var pillPanel: RecordingPillPanel?
       - private var stateObservation: AnyCancellable?

       In applicationDidFinishLaunching (after existing code):
       a. Configure HotkeyService.onArm = { [weak self] in
              Task { @MainActor in self?.activationStore.arm() }
          }
       b. Try audioCapture.prepare() — log error if it fails, do not crash (mic permission may not be granted yet)
       c. If readinessStore.snapshot.state == .ready: hotkeyService.start()
          Otherwise: observe readinessStore.$snapshot and start hotkeyService when state becomes .ready
       d. Create pillPanel = RecordingPillPanel(levelMonitor: levelMonitor) — create once, do not show yet
       e. Subscribe to activationStore.$state (using Combine sink or async Task):
          - On .recording:
            * Apply device selection: find AudioInputDevice matching preferences.micDeviceUID
              via audioDevice.availableDevices; call audioDevice.setInputDevice(device, on: engine)
              before start — NOTE: AudioCaptureService.start() should internally apply device selection
              via AudioDeviceService before starting engine. Update AudioCaptureService.start() signature
              if needed: start(levelMonitor:, deviceUID: String?) — applies device then starts.
            * try audioCapture.start(levelMonitor: levelMonitor, deviceUID: preferences.micDeviceUID)
            * pillPanel?.orderFrontRegardless()
            * Update menu bar icon to recording state (see step 4)
            * Register disconnect listener: if let uid = preferences.micDeviceUID {
                  audioDevice.registerDisconnectListener(for: uid) { [weak self] in
                      Task { @MainActor in
                          self?.audioCapture.stop()
                          // Re-start with system default
                          try? self?.audioCapture.start(levelMonitor: self!.levelMonitor, deviceUID: nil)
                      }
                  }
              }
          - On .idle:
            * audioCapture.stop()
            * pillPanel?.orderOut(nil)
            * levelMonitor.reset()
            * Restore menu bar icon to idle state

    4. Menu bar icon state change:
       - The app uses NSStatusItem in StatusMenuView or AppDelegate (check Phase 1 implementation)
       - When recording: set statusItem.button?.image = NSImage(systemSymbolName: "mic.fill", accessibilityDescription: "Recording") — or use a template image named "MenuBarRecording" if one is added to Assets
       - When idle: restore to original icon (the mic icon used in Phase 1)
       - Use Claude's discretion for exact icon variant per CONTEXT.md

    5. Update AudioCaptureService.start() signature to accept deviceUID:
       - func start(levelMonitor: AudioLevelMonitor, deviceUID: String?) throws
       - If deviceUID is non-nil and non-empty: look up AudioDeviceID via AudioDeviceService.shared
         and call setInputDevice before engine.start()
       - This keeps device selection logic coordinated in one place

    Anti-patterns to avoid:
    - Do NOT create pillPanel on every activation — create once in applicationDidFinishLaunching
    - Do NOT connect inputNode to outputNode (covered in AudioCaptureService but reinforce here)
    - Do NOT call AudioUnitSetProperty after engine.start() — stop first if device must change
  </action>
  <verify>
    Build succeeds:
    xcodebuild build -scheme Speech2Test -destination 'platform=macOS' 2>&1 | tail -20
  </verify>
  <done>
    - AppDelegate wires ActivationStore state changes to pill show/hide, audio start/stop, icon change.
    - HotkeyService.start() is called when app is ready.
    - Activation sound plays via system sound ID 1057 when activationSoundEnabled is true.
    - Disconnect listener falls back to system default silently.
    - Build is clean with no new warnings or errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full Phase 2 activation loop</name>
  <action>Human verifies the complete activation flow end-to-end as described below.</action>
  <verify>Human confirms all verification steps below pass.</verify>
  <done>All 9 verification steps pass and user types "approved".</done>
  <what-built>
    Complete Phase 2 activation loop:
    - Hotkey (Cmd+Shift+Z by default) starts recording from any app
    - Floating pill appears at bottom center, above all windows, with mic icon and animated waveform
    - Activation sound plays on recording start
    - Menu bar icon changes to recording state
    - Pill and recording state visible on all Spaces
  </what-built>
  <how-to-verify>
    Prerequisites: App built and running, microphone and accessibility permissions granted.

    1. Launch Speech2Test. Verify: menu bar icon is idle state (mic icon, not recording variant).
    2. Open a different application (e.g., TextEdit) so Speech2Test is in the background.
    3. Double-tap Cmd+Shift+Z within 350ms. Verify:
       a. A subtle click sound plays (system sound 1057).
       b. The floating pill appears at the bottom center of the screen.
       c. The pill shows a mic icon and animated waveform bars.
       d. The menu bar icon changes to the recording variant.
       e. TextEdit remains frontmost — app focus is NOT stolen.
    4. Open a new Space or Mission Control space. Verify pill is visible on the new Space.
    5. Open a full-screen app (e.g., full-screen Safari). Double-tap Cmd+Shift+Z. Verify pill appears above the full-screen app.
    6. Single-tap Cmd+Shift+Z in double-tap mode. Verify: nothing happens (no sound, no pill).
    7. Open Speech2Test Setup window. Change tap mode to "Single Tap". Single-tap Cmd+Shift+Z. Verify recording starts immediately.
    8. Open mic picker in setup window. Verify "System Default" appears first, then available mics.
    9. In setup window, disable Activation Sound. Trigger recording. Verify no sound plays.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which checks failed with specific observed behavior</resume-signal>
</task>

</tasks>

<verification>
Final full test suite:

```
xcodebuild test -scheme Speech2Test -destination 'platform=macOS' 2>&1 | tail -30
```

All Phase 1 tests + Phase 2 Plan 01 + Plan 02 tests must pass. Build must be clean.
</verification>

<success_criteria>
- Hotkey fires, pill appears at bottom center, mic icon + animated waveform visible.
- Pill appears on all Spaces and above full-screen apps.
- Menu bar icon transitions idle ↔ recording with ActivationStore.state.
- Activation sound plays on arm (system sound 1057), silenced when toggled off.
- Single-tap in double-tap mode: silently ignored.
- Full test suite green. Build clean.
- Human verification approved.
</success_criteria>

<output>
After completion, create .planning/phases/02-activation-and-capture/02-03-SUMMARY.md following the template at .codex/get-shit-done/templates/summary.md
</output>
