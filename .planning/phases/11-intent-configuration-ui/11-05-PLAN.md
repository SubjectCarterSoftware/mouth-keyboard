---
phase: 11-intent-configuration-ui
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified: []
autonomous: false
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03

must_haves:
  truths:
    - "Settings panel shows all 6 built-in modes with correct names and prompt previews"
    - "User can edit a built-in mode's system prompt and save — live app applies the override on next dictation"
    - "Reset to Default restores ConvertMode.defaultSystemPrompt for a built-in mode"
    - "User can create a custom mode with name + system prompt; mode appears in list after save"
    - "Custom mode triggers a dictation rewrite in the live app when the user speaks the mode name"
    - "Deleting a custom mode removes it from the list; it no longer triggers rewrites"
    - "Ghost text name suggestion appears in the name field while typing a system prompt"
    - "Live preview panel shows LLM output after ~2s of idle system prompt editing"
    - "Phrase trigger tester (expanded) shows triggered/not triggered for typed phrases"
  artifacts:
    - path: "Speech2Text/Shell/IntentListView.swift"
      provides: "Visually correct and functional"
      min_lines: 50
    - path: "Speech2Text/Shell/IntentEditView.swift"
      provides: "Split pane with all interactive elements working"
      min_lines: 80
  key_links:
    - from: "Custom mode created in UI"
      to: "ActivationStore.finalizeSession -> rewrite(body:instructions:)"
      via: "UserIntentStore.shared -> IntentCatalog.effective -> IntentDetector.detect(definitions:)"
      pattern: "end-to-end: UI save -> store -> activation"
---

<objective>
Manual verification of the complete Phase 11 feature surface in the running macOS app.

Purpose: Unit tests cover the data layer and pipeline wiring. These behaviors require live LLM inference, SwiftUI rendering, and end-to-end dictation flow — they cannot be automated in the CI suite.

Output: Signed-off verification checklist. No code changes expected; if bugs are found, they are fixed before sign-off.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-intent-configuration-ui/11-CONTEXT.md
@.planning/phases/11-intent-configuration-ui/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build app and run final unit gate</name>
  <files>.</files>
  <action>
    Run the full unit suite one final time to confirm no regressions from Plan 04 UI additions:
    `xcodebuild test -scheme Speech2Text -destination 'platform=macOS' -only-testing:Speech2TextTests 2>&1 | tail -10`

    Then build the full app (required for manual verification):
    `xcodebuild build -scheme Speech2Text -destination 'platform=macOS' 2>&1 | tail -5`

    If both succeed, launch the app via the built product or Xcode and confirm the menu bar item appears.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Speech2Text -destination 'platform=macOS' 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED"</automated>
  </verify>
  <done>Full test suite green; app builds successfully and launches with menu bar item visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — intent configuration UI end-to-end</name>
  <what-built>
    Complete Phase 11 intent configuration UI:
    - IntentListView showing all built-in and custom modes
    - IntentEditView split pane: live preview (2s debounce), ghost text name suggestion (1s debounce), phrase tester (DisclosureGroup), Reset/Delete/Save buttons
    - Background phrase pattern generation (silent — no UI indicator)
    - ActivationStore wired to UserIntentStore for effective prompt resolution at session-start
    - UserIntentStore JSON persistence in App Support/Speech2Text/IntentStore.json
  </what-built>
  <how-to-verify>
    Open the Settings / Setup window and navigate to the Modes tab, then verify each item:

    **CONFIG-01: View all modes**
    1. Open app Settings → Modes tab
    2. Confirm: 6 rows visible (Clean English, Email, Slack, Teams, Action Items, AI Prompt)
    3. Confirm: each row shows mode name + truncated system prompt preview text
    4. Expected: no custom modes yet (unless added in earlier tests)

    **CONFIG-03: Edit built-in + Reset**
    5. Tap "Email" row → edit view opens with current system prompt pre-filled
    6. Edit the system prompt (e.g., append "Keep it brief.")
    7. Confirm: after ~1s, a name suggestion appears as ghost text in the name field
    8. Confirm: after ~2s, the right panel shows updated LLM preview output
    9. Tap Save → return to list → Email row shows updated preview
    10. Reopen Email row → tap "Reset to Default"
    11. Confirm: system prompt reverts to the original Email default prompt
    12. Return to list → Email row shows original preview text

    **CONFIG-02: Create custom mode**
    13. Tap "Add Mode" → edit view opens with empty name and prompt
    14. Type a system prompt (e.g., "Convert the dictated text to a formal meeting summary with attendees, decisions, and action items.")
    15. Confirm: after ~1s, a mode name suggestion appears as ghost text (e.g., "Meeting Summary")
    16. Confirm: after ~2s, the right panel shows LLM preview output
    17. Set the mode name (accept suggestion or type your own)
    18. Tap Save → return to list → custom mode appears as a new row

    **CONFIG-02: Custom mode triggers dictation rewrite (end-to-end)**
    19. Dictate: "had a call with the team today convert to meeting summary" (use the hotkey)
    20. Confirm: clipboard contains a rewritten meeting summary (not the raw dictation text)
    21. PASS: custom mode triggered correctly end-to-end through UserIntentStore → ActivationStore → LLMRewriteService

    **CONFIG-03: Delete custom mode**
    22. Tap the custom mode row → confirm: Delete button is visible, Reset button is NOT visible
    23. Tap Delete → return to list → custom mode is gone
    24. Dictate the same phrase — confirm: passthrough (raw transcript copied, not rewritten)

    **Phrase trigger tester**
    25. Create or edit any mode → expand "Test trigger phrase" disclosure
    26. Type a phrase related to the mode (e.g., "turn this into a meeting summary")
    27. Confirm: result shows "Triggered as [mode name]" or "Not triggered"
    28. Note: if patterns show "Not triggered" immediately after creating a mode, wait ~30s for background generation and re-test
  </how-to-verify>
  <action>Observe live app behavior for all test scenarios above. Fix any bugs found before approving.</action>
  <verify>
    <automated>xcodebuild test -scheme Speech2Text -destination 'platform=macOS' -only-testing:Speech2TextTests 2>&1 | tail -3</automated>
  </verify>
  <done>All manual scenarios pass; no crashes; custom mode end-to-end verified; full test suite green.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any failures found</resume-signal>
</task>

</tasks>

<verification>
All manual verification items above confirmed by user.
Full test suite green before sign-off.
</verification>

<success_criteria>
- All CONFIG-01, CONFIG-02, CONFIG-03 behaviors verified in the running app
- No crashes or data loss during edit/save/reset/delete operations
- Custom mode end-to-end: create → save → dictation triggers rewrite → delete → passthrough
- Ghost text name suggestion visible during system prompt editing
- Live preview updates after ~2s idle system prompt editing
- Phrase tester responds to typed phrases (may require waiting for background generation)
</success_criteria>

<output>
After completion, create `.planning/phases/11-intent-configuration-ui/11-05-SUMMARY.md`
</output>
