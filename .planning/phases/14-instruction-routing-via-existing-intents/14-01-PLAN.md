---
phase: 14-instruction-routing-via-existing-intents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Speech2Text/Conversion/IntentDetector.swift
  - Speech2TextTests/IntentDetectorTests.swift
  - Speech2Text/Activation/ActivationStore.swift
  - Speech2TextTests/ActivationStoreTests.swift
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04

must_haves:
  truths:
    - "Post-trigger instruction routing evaluates existing predefined intents with conservative fuzzy matching and end-position gating."
    - "Predefined shortcut routing executes only on clear, high-confidence winners; ambiguous and mixed-intent instructions do not force built-in mode conversion."
    - "Routing decision remains deterministic and test-driven for trailing-only shortcut behavior."
  artifacts:
    - path: "Speech2Text/Conversion/IntentDetector.swift"
      provides: "Conservative predefined shortcut resolver for post-trigger instruction segments"
      exports: ["IntentDetector"]
    - path: "Speech2TextTests/IntentDetectorTests.swift"
      provides: "Deterministic corpus for end-position-only shortcut matching and ambiguity handling"
      contains: "testTriggerInstruction"
    - path: "Speech2TextTests/ActivationStoreTests.swift"
      provides: "Finalize-path integration assertions that built-in shortcut wins still use existing prompt pipeline"
      contains: "test_finalize"
  key_links:
    - from: "Speech2Text/Activation/ActivationStore.swift"
      to: "Speech2Text/Conversion/IntentDetector.swift"
      via: "validTrigger instruction segment is routed through conservative predefined shortcut detection"
      pattern: "validTrigger|detect"
---

<objective>
Establish deterministic shortcut routing for valid post-trigger instruction text, so existing predefined intents are selected only on clear trailing command matches.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-instruction-routing-via-existing-intents/14-CONTEXT.md
@Speech2Text/Activation/ActivationStore.swift
@Speech2Text/Conversion/IntentDetector.swift
@Speech2TextTests/IntentDetectorTests.swift
@Speech2TextTests/ActivationStoreTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED corpus for conservative trailing shortcut resolution and ambiguity fallback</name>
  <files>
    Speech2TextTests/IntentDetectorTests.swift,
    Speech2TextTests/ActivationStoreTests.swift
  </files>
  <action>
    Add failing tests that lock Phase 14 shortcut policy:
    - post-trigger instructions only match built-in shortcuts when command phrasing is at the end
    - mixed intent instructions (shortcut words plus extra style directives) do not hard-route to built-in modes
    - ambiguous tie/near-tie scoring between built-ins does not select a built-in mode
    - clear trailing built-in instructions still resolve to the expected predefined mode
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/IntentDetectorTests -only-testing Speech2TextTests/ActivationStoreTests -destination 'platform=macOS'</verify>
  <done>
    - RED tests define conservative routing behavior for trailing-only shortcuts
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement conservative predefined shortcut resolver and wire valid-trigger routing to it</name>
  <files>
    Speech2Text/Conversion/IntentDetector.swift,
    Speech2Text/Activation/ActivationStore.swift,
    Speech2TextTests/IntentDetectorTests.swift,
    Speech2TextTests/ActivationStoreTests.swift
  </files>
  <action>
    Implement detector and integration updates:
    - enforce end-position shortcut qualification for post-trigger instruction routing
    - apply high-confidence threshold and near-tie rejection for built-in shortcut selection
    - keep unresolved/ambiguous valid-trigger instructions on the non-shortcut path for Plan 14-02 fallback
    - preserve existing built-in execution path semantics when a clear shortcut match is found
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/IntentDetectorTests -only-testing Speech2TextTests/ActivationStoreTests -destination 'platform=macOS'</verify>
  <done>
    - deterministic trailing shortcut resolver is active for valid-trigger instructions and tests are green
  </done>
</task>

</tasks>
