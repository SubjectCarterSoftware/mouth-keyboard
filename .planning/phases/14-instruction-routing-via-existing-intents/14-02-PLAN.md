---
phase: 14-instruction-routing-via-existing-intents
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - Speech2Text/Activation/ActivationStore.swift
  - Speech2TextTests/ActivationStoreTests.swift
  - Speech2TextTests/IntentDetectorTests.swift
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04

must_haves:
  truths:
    - "When valid-trigger instruction does not clearly match a predefined shortcut, ActivationStore routes instruction text to custom rewrite instructions path instead of raw passthrough."
    - "Built-in shortcut winners continue to run through existing predefined prompt pipeline."
    - "Guard behavior remains unchanged in trigger-based flows: 350-word gate blocks conversion and LLM failures silently fall back to raw transcript clipboard output."
    - "End-to-end trigger transcript tests cover both predefined shortcut path and custom-instruction fallback path."
  artifacts:
    - path: "Speech2Text/Activation/ActivationStore.swift"
      provides: "Finalize-time routing split between predefined shortcut execution and custom instruction fallback"
      contains: "finalizeSession"
    - path: "Speech2TextTests/ActivationStoreTests.swift"
      provides: "Integration regressions for shortcut route, custom fallback route, word-limit gate, and silent failure fallback"
      contains: "test_finalize"
    - path: "Speech2TextTests/IntentDetectorTests.swift"
      provides: "Detector-level regression coverage for unresolved shortcut cases that should feed custom fallback"
      contains: "testTriggerInstruction"
  key_links:
    - from: "Speech2Text/Activation/ActivationStore.swift"
      to: "Speech2Text/Conversion/IntentDetector.swift"
      via: "No-clear-shortcut detector outputs map to custom instruction rewrite calls for valid-trigger transcripts"
      pattern: "validTrigger|passthrough|instructions"
    - from: "Speech2Text/Activation/ActivationStore.swift"
      to: "Speech2Text/Conversion/LLMRewriteService.swift"
      via: "Fallback path uses rewrite(body:instructions:) while preserving guard/failure contracts"
      pattern: "rewrite"
---

<objective>
Complete trigger instruction routing by adding custom-instruction fallback for unmatched valid-trigger instructions while preserving word-limit and silent-fallback guarantees.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-instruction-routing-via-existing-intents/14-CONTEXT.md
@.planning/phases/14-instruction-routing-via-existing-intents/14-01-PLAN.md
@Speech2Text/Activation/ActivationStore.swift
@Speech2TextTests/ActivationStoreTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED integration tests for valid-trigger custom fallback and guard preservation</name>
  <files>
    Speech2TextTests/ActivationStoreTests.swift,
    Speech2TextTests/IntentDetectorTests.swift
  </files>
  <action>
    Add failing tests that assert:
    - valid-trigger + clear built-in shortcut still uses predefined mode pipeline
    - valid-trigger + no clear built-in shortcut invokes rewrite(body:instructions:) with post-trigger instruction text as custom instructions
    - 350-word gate still blocks conversion for trigger-routed paths and writes raw transcript
    - LLM failure during both shortcut/custom trigger routes still silently falls back to raw transcript clipboard output
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/ActivationStoreTests -only-testing Speech2TextTests/IntentDetectorTests -destination 'platform=macOS'</verify>
  <done>
    - RED integration coverage locks ROUTE-03 and ROUTE-04 behavior from spoken transcript inputs
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ActivationStore custom instruction fallback for unresolved valid-trigger instructions</name>
  <files>
    Speech2Text/Activation/ActivationStore.swift,
    Speech2TextTests/ActivationStoreTests.swift,
    Speech2TextTests/IntentDetectorTests.swift
  </files>
  <action>
    Update finalize-time routing:
    - keep built-in shortcut winners on current predefined prompt path
    - when valid-trigger instruction has no clear shortcut match, call rewrite(body:instructions:) using content segment as body and post-trigger instruction as custom instructions
    - retain existing no-trigger/invalid-trigger passthrough behavior unchanged
    - preserve existing guard semantics (word-limit fail + silent raw fallback on rewrite errors) across shortcut and custom fallback flows
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/ActivationStoreTests -only-testing Speech2TextTests/IntentDetectorTests -only-testing Speech2TextTests/TriggerTranscriptParserTests -destination 'platform=macOS'</verify>
  <done>
    - trigger-based routing supports predefined shortcuts and custom fallback with all targeted regressions green
  </done>
</task>

</tasks>
