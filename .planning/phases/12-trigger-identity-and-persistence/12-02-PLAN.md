---
phase: 12-trigger-identity-and-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - Speech2Text/Activation/TriggerAliasNormalizer.swift
  - Speech2Text/Activation/TriggerCalibrationSession.swift
  - Speech2Text/Activation/TriggerProfileStore.swift
  - Speech2Text/Persistence/ShellPreferences.swift
  - Speech2Text/Activation/ActivationStore.swift
  - Speech2TextTests/TriggerAliasNormalizerTests.swift
  - Speech2TextTests/TriggerCalibrationSessionTests.swift
  - Speech2TextTests/TriggerProfileStoreTests.swift
  - Speech2Text.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - TRIG-03
  - TRIG-04

must_haves:
  truths:
    - "Calibration contract requires three valid samples for the active trigger profile and retries invalid/empty captures instead of accepting partial runs."
    - "Alias normalization output is lowercase, trimmed, whitespace-collapsed, deduplicated by exact normalized value, and rejects entries shorter than 2 characters."
    - "Re-running calibration replaces prior aliases for the active profile rather than merging historical aliases."
    - "Alias sets remain profile-scoped: recalibrating one profile does not mutate aliases for other predefined or custom profiles."
    - "Calibration persistence updates are immediately available to runtime consumers and survive relaunch with no restart-only path."
  artifacts:
    - path: "Speech2Text/Activation/TriggerAliasNormalizer.swift"
      provides: "Deterministic alias normalization and validation pipeline used by calibration and custom-name persistence"
      exports: ["TriggerAliasNormalizer"]
    - path: "Speech2Text/Activation/TriggerCalibrationSession.swift"
      provides: "Calibration capture contract enforcing three valid samples and retry semantics"
      exports: ["TriggerCalibrationSession", "CalibrationSample"]
    - path: "Speech2Text/Activation/TriggerProfileStore.swift"
      provides: "Profile-scoped alias replacement writes for completed calibration runs"
      contains: "replaceAliasesForActiveProfile"
    - path: "Speech2TextTests/TriggerAliasNormalizerTests.swift"
      provides: "RED->GREEN coverage for normalization, dedupe, minimum-length rejection, and multi-word aliases"
      contains: "testNormalize"
    - path: "Speech2TextTests/TriggerCalibrationSessionTests.swift"
      provides: "Contract tests for three-sample completion, retry behavior, and replacement semantics"
      contains: "testRequiresThreeValidSamples"
  key_links:
    - from: "Speech2Text/Activation/TriggerCalibrationSession.swift"
      to: "Speech2Text/Activation/TriggerAliasNormalizer.swift"
      via: "Captured samples are normalized and validated before profile persistence writes"
      pattern: "completeCalibration|normalize"
    - from: "Speech2Text/Persistence/ShellPreferences.swift"
      to: "Speech2Text/Activation/TriggerProfileStore.swift"
      via: "Calibration completion replaces aliases for active profile and publishes updated trigger state"
      pattern: "applyCalibrationAliases|save"
    - from: "Speech2Text/Activation/ActivationStore.swift"
      to: "Speech2Text/Persistence/ShellPreferences.swift"
      via: "Finalize-time runtime reads include latest normalized alias contract with no app relaunch"
      pattern: "activeTriggerProfile|finalizeSession"
---

<objective>
Deliver a test-first calibration capture contract and alias normalization pipeline so Phase 12 persists reliable trigger aliases per profile.

Purpose: After Plan 01 establishes profile persistence, this plan defines how spoken calibration samples become normalized aliases with deterministic acceptance/rejection and profile-scoped replacement behavior.

Output: normalization utility, calibration session contract, profile alias replacement integration, and comprehensive unit coverage proving CAL-01/CAL-02 behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-trigger-identity-and-persistence/12-CONTEXT.md
@.planning/phases/12-trigger-identity-and-persistence/12-01-PLAN.md
@Speech2Text/Persistence/ShellPreferences.swift
@Speech2Text/Activation/TriggerProfileStore.swift
@Speech2Text/Activation/ActivationStore.swift
</context>

<interfaces>
Calibration pipeline should stay deterministic and testable outside UI:

```swift
struct CalibrationSample: Equatable {
    let rawTranscript: String
}
```

```swift
enum TriggerAliasNormalizer {
    static func normalize(_ values: [String]) -> [String]
}
```

```swift
struct TriggerCalibrationSession {
    mutating func recordSample(_ sample: CalibrationSample?) -> CalibrationStepResult
    var isComplete: Bool { get }
    func finalizedAliases() -> [String]
}
```

The executor should keep capture/normalization logic UI-agnostic so later settings flows can consume the same contract without duplicating rules.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write RED tests for normalization and calibration contract before implementation</name>
  <files>
    Speech2TextTests/TriggerAliasNormalizerTests.swift,
    Speech2TextTests/TriggerCalibrationSessionTests.swift,
    Speech2TextTests/TriggerProfileStoreTests.swift,
    Speech2Text.xcodeproj/project.pbxproj
  </files>
  <action>
    Define failing tests first to lock Phase 12 calibration semantics before production changes.

    RED test requirements:
    - `TriggerAliasNormalizerTests`:
      - lowercases and trims aliases
      - collapses internal whitespace for multi-word values
      - deduplicates by exact post-normalization value
      - rejects aliases with normalized length < 2
      - keeps valid multi-word aliases
    - `TriggerCalibrationSessionTests`:
      - calibration requires three valid samples before completion
      - nil/empty/noise sample results request retry and do not advance completion count
      - completed session returns normalized aliases containing canonical primary name
      - re-running calibration replaces prior alias set for the active profile
    - `TriggerProfileStoreTests` extension:
      - alias writes are scoped to active profile and do not mutate non-active profile aliases
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/TriggerAliasNormalizerTests -only-testing Speech2TextTests/TriggerCalibrationSessionTests -only-testing Speech2TextTests/TriggerProfileStoreTests -destination 'platform=macOS'</verify>
  <done>
    - failing tests precisely encode CAL-01/CAL-02 semantics
    - replacement and profile-scope rules are captured before implementation
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement normalization and calibration persistence pipeline to make RED suite pass</name>
  <files>
    Speech2Text/Activation/TriggerAliasNormalizer.swift,
    Speech2Text/Activation/TriggerCalibrationSession.swift,
    Speech2Text/Activation/TriggerProfileStore.swift,
    Speech2Text/Persistence/ShellPreferences.swift,
    Speech2Text/Activation/ActivationStore.swift,
    Speech2TextTests/TriggerAliasNormalizerTests.swift,
    Speech2TextTests/TriggerCalibrationSessionTests.swift,
    Speech2TextTests/TriggerProfileStoreTests.swift
  </files>
  <action>
    Implement the production contract required by the RED tests and phase context decisions.

    Implementation requirements:
    - Add `TriggerAliasNormalizer` as pure deterministic logic following the canonical normalization rules from `12-CONTEXT.md`.
    - Add calibration session coordinator that accepts sample attempts, enforces three valid captures, and emits retry signals for invalid attempts.
    - Ensure calibration completion writes alias replacements for the active profile only (replace, do not merge).
    - Preserve custom-profile restore semantics from Plan 01 when calibration updates custom aliases.
    - Publish updated trigger profile state through `ShellPreferences` so subsequent sessions observe new aliases immediately.
    - Keep `ActivationStore` read path aligned with updated alias contract without introducing parser behavior from Phase 13.
  </action>
  <verify>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -only-testing Speech2TextTests/TriggerAliasNormalizerTests -only-testing Speech2TextTests/TriggerCalibrationSessionTests -only-testing Speech2TextTests/TriggerProfileStoreTests -only-testing Speech2TextTests/ActivationStoreTests -destination 'platform=macOS'</verify>
  <done>
    - alias normalization contract is implemented and fully tested
    - three-sample calibration capture semantics are enforced
    - alias replacement is profile-scoped and relaunch-persistent
    - runtime consumers see normalized aliases immediately after calibration updates
  </done>
</task>

</tasks>
