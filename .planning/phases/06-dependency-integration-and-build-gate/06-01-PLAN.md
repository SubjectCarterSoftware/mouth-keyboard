---
phase: 06-dependency-integration-and-build-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Speech2Text.xcodeproj/project.pbxproj
  - Speech2Text.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - build.sh
autonomous: false

requirements:
  - LLM-01
  - LLM-02

must_haves:
  truths:
    - "xcodebuild -resolvePackageDependencies succeeds on a clean checkout (no Package.resolved present)"
    - "The app builds via xcodebuild with no missing-symbol or Metal-shader errors"
    - "Plain dictation (no trigger phrase) still copies raw transcript to clipboard unchanged"
    - "build.sh at the repo root documents the xcodebuild-only constraint with an inline comment"
  artifacts:
    - path: "build.sh"
      provides: "xcodebuild wrapper script with Metal constraint documentation"
      contains: "xcodebuild"
    - path: "Speech2Text.xcodeproj/project.pbxproj"
      provides: "Updated Xcode project with mlx-swift-lm SPM reference and MLXLLM/MLXLMCommon product links"
      contains: "mlx-swift-lm"
    - path: "Speech2Text.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved"
      provides: "Pinned mlx-swift-lm 2.30.6 and mlx-swift 0.30.6 entries"
      contains: "mlx-swift-lm"
  key_links:
    - from: "Speech2Text.xcodeproj/project.pbxproj"
      to: "https://github.com/ml-explore/mlx-swift-lm"
      via: "XCRemoteSwiftPackageReference"
      pattern: "mlx-swift-lm"
    - from: "build.sh"
      to: "Speech2Text.xcodeproj"
      via: "xcodebuild -project flag"
      pattern: "xcodebuild.*Speech2Text\\.xcodeproj"
---

<objective>
Add mlx-swift-lm 2.30.6 as an SPM dependency to the Xcode project, commit the resolved Package.resolved, and create build.sh documenting the xcodebuild-only build requirement.

Purpose: Phase 8 (LLMRewriteService) requires MLXLLM and MLXLMCommon to be present in the project. Phase 6 gets the dependency in place and gates on a clean build + regression smoke test before any LLM service code is written.
Output: Updated project.pbxproj with mlx-swift-lm reference, updated Package.resolved with mlx-swift-lm and mlx-swift pins, and build.sh at the repo root.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dependency-integration-and-build-gate/06-CONTEXT.md
@.planning/phases/06-dependency-integration-and-build-gate/06-RESEARCH.md

<interfaces>
<!-- Existing XCRemoteSwiftPackageReference pattern from project.pbxproj (lines 756-775) -->
<!-- Use this pattern for the new mlx-swift-lm reference — same structure, different UUID/URL/requirement -->

Existing dependency pattern:
```
A00000140000000000000002 /* XCRemoteSwiftPackageReference "WhisperKit" */ = {
    isa = XCRemoteSwiftPackageReference;
    repositoryURL = "https://github.com/argmaxinc/WhisperKit";
    requirement = {
        kind = upToNextMajorVersion;
        minimumVersion = 0.17.0;
    };
};
A00000130000000000000002 /* WhisperKit */ = {
    isa = XCSwiftPackageProductDependency;
    package = A00000140000000000000002 /* XCRemoteSwiftPackageReference "WhisperKit" */;
    productName = WhisperKit;
};
```

mlx-swift-lm uses `upToNextMinorVersion` (not upToNextMajorVersion).
Two product dependency entries needed: MLXLLM and MLXLMCommon.

Correct URL: https://github.com/ml-explore/mlx-swift-lm
Version rule: upToNextMinorVersion from 2.30.6

No conflict: WhisperKit 0.17.0 and mlx-swift-lm 2.30.6 both pin swift-transformers
to .upToNextMinor(from: "1.1.6"). Current Package.resolved already has 1.1.9. No resolution needed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Add mlx-swift-lm via Xcode SPM UI</name>
  <files>Speech2Text.xcodeproj/project.pbxproj, Speech2Text.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved</files>
  <action>
    Open the Xcode project and add mlx-swift-lm as a Swift Package dependency:

    1. Open Speech2Text.xcodeproj in Xcode
    2. File > Add Package Dependencies
    3. In the search field, enter: https://github.com/ml-explore/mlx-swift-lm
    4. Set Dependency Rule to: "Up to Next Minor Version" from "2.30.6"
    5. Click Add Package
    6. In the product selection sheet, check ONLY: MLXLLM and MLXLMCommon (do NOT check MLXVLM or MLXEmbedders)
    7. Ensure "Add to Target: Speech2Text" is selected
    8. Click Add Package

    After adding:
    - Xcode will resolve packages and update Package.resolved automatically
    - Verify that project.pbxproj now contains an XCRemoteSwiftPackageReference for mlx-swift-lm
    - Verify that Package.resolved contains entries for mlx-swift-lm (2.30.6) and mlx-swift (0.30.6)
    - swift-transformers should remain pinned at 1.1.9 (no conflict — both packages use upToNextMinor from 1.1.6)

    IMPORTANT: Do NOT add mlx-swift as a direct dependency — it is a transitive dependency of mlx-swift-lm and SPM resolves it automatically.

    If Xcode's SPM UI is unavailable, edit project.pbxproj directly following the existing XCRemoteSwiftPackageReference pattern shown in the context interfaces block above. Use upToNextMinorVersion with minimumVersion = 2.30.6.
  </action>
  <verify>
    <automated>grep -l "mlx-swift-lm" /Users/elicarter/Workspace/speech2test/Speech2Text.xcodeproj/project.pbxproj && grep -l "mlx-swift-lm" "/Users/elicarter/Workspace/speech2test/Speech2Text.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved" && echo "Dependency present"</automated>
  </verify>
  <done>project.pbxproj contains an XCRemoteSwiftPackageReference for mlx-swift-lm; Package.resolved contains mlx-swift-lm 2.30.6 and mlx-swift 0.30.6 pins; MLXLLM and MLXLMCommon are linked to the Speech2Text target</done>
  <how-to-verify>
    In Xcode, open the project navigator > Package Dependencies — mlx-swift-lm 2.30.6 should appear alongside WhisperKit and KeyboardShortcuts. Also inspect Package.resolved and confirm mlx-swift-lm and mlx-swift pins are present.
  </how-to-verify>
  <resume-signal>Type "done" once mlx-swift-lm appears in Xcode's Package Dependencies list and Package.resolved has been updated</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write build.sh</name>
  <files>build.sh</files>
  <action>
    Create build.sh at the repo root. The file must:

    1. Start with #!/bin/bash
    2. Include an inline block comment explaining the xcodebuild-only constraint:
       - Metal shaders (default.metallib) are only compiled by Xcode's build system
       - `swift build` silently omits Metal compilation, causing runtime failures
       - This applies to any Metal-backed framework, including mlx-swift for LLM GPU inference
    3. Include a comment documenting the clean-resolution test procedure:
       - rm Speech2Text.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
       - xcodebuild -project Speech2Text.xcodeproj -resolvePackageDependencies
    4. Include `set -euo pipefail`
    5. Run the xcodebuild build command with these exact flags:
       - -project Speech2Text.xcodeproj
       - -scheme Speech2Text
       - -destination 'platform=macOS,arch=arm64'
       - -configuration Release
       - build (not test, not archive)

    Make the script executable: chmod +x build.sh

    The exact comment wording is at your discretion, but all four pieces of documented information above must appear.
  </action>
  <verify>
    <automated>test -x /Users/elicarter/Workspace/speech2test/build.sh && grep -q "xcodebuild" /Users/elicarter/Workspace/speech2test/build.sh && grep -q "default.metallib" /Users/elicarter/Workspace/speech2test/build.sh && grep -q "resolvePackageDependencies" /Users/elicarter/Workspace/speech2test/build.sh && echo "build.sh OK"</automated>
  </verify>
  <done>build.sh exists at repo root, is executable, contains the xcodebuild command with correct flags, the Metal shader constraint comment, and the clean-resolution procedure comment</done>
</task>

<task type="auto">
  <name>Task 3: Verify package resolution and build</name>
  <files></files>
  <action>
    Run package dependency resolution to confirm all packages resolve cleanly:

    ```bash
    xcodebuild \
        -project Speech2Text.xcodeproj \
        -resolvePackageDependencies
    ```

    Then run a full build:

    ```bash
    xcodebuild \
        -project Speech2Text.xcodeproj \
        -scheme Speech2Text \
        -destination 'platform=macOS,arch=arm64' \
        -configuration Release \
        build
    ```

    Expect: Build succeeded with no errors. Warnings are acceptable.

    If resolvePackageDependencies produces a swift-transformers version conflict error (unexpected given research), surface the error output and stop — do not proceed to the build step. The conflict resolution strategy is documented in 06-CONTEXT.md (bump WhisperKit version or fall back to mlx-swift directly).

    Then run the existing unit test suite to confirm no regressions:

    ```bash
    xcodebuild \
        -project Speech2Text.xcodeproj \
        -scheme Speech2Text \
        -destination 'platform=macOS,arch=arm64' \
        test
    ```

    Expect: All existing tests pass.
  </action>
  <verify>
    <automated>cd /Users/elicarter/Workspace/speech2test && xcodebuild -project Speech2Text.xcodeproj -resolvePackageDependencies 2>&1 | grep -i "error" | grep -v "warning" | head -5; xcodebuild -project Speech2Text.xcodeproj -scheme Speech2Text -destination 'platform=macOS,arch=arm64' -configuration Release build 2>&1 | tail -3</automated>
  </verify>
  <done>xcodebuild reports "BUILD SUCCEEDED" and all existing unit tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke test — plain dictation regression</name>
  <files></files>
  <action>Human verification required: launch the app and confirm plain dictation still works correctly with no regression from adding mlx-swift-lm.</action>
  <what-built>mlx-swift-lm 2.30.6 added to the project, project builds cleanly, unit tests green</what-built>
  <how-to-verify>
    1. Launch the Speech2Text app (run from Xcode or open the built .app)
    2. Press the activation hotkey to start recording
    3. Dictate a short phrase (e.g. "This is a test of plain dictation")
    4. Press the activation hotkey again to finish
    5. Paste into a text field — confirm the raw transcript appears unchanged (no extra processing, no modification)
    6. Latency check: confirm the microphone recording indicator appears within ~200ms of pressing the hotkey (no perceptible added delay from the new dependency loading at launch)
  </how-to-verify>
  <verify>
    <automated>MISSING — manual smoke test required; no automated command can verify clipboard output from a live dictation session</automated>
  </verify>
  <done>Plain dictation copies raw transcript to clipboard unchanged; recording starts within ~200ms of hotkey press; no regression from adding mlx-swift-lm</done>
  <resume-signal>Type "approved" if plain dictation works correctly, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
Phase gate: ALL of the following must be true before marking Phase 6 complete:

1. xcodebuild -resolvePackageDependencies completes without errors
2. xcodebuild ... build reports BUILD SUCCEEDED
3. xcodebuild ... test reports all existing tests pass
4. build.sh exists at repo root, is executable, contains the Metal shader constraint comment
5. Package.resolved contains mlx-swift-lm 2.30.6 and mlx-swift 0.30.6 pins
6. Manual smoke test: plain dictation copies raw transcript to clipboard unchanged
</verification>

<success_criteria>
- `xcodebuild -resolvePackageDependencies` succeeds on a clean checkout (Package.resolved deleted and re-resolved)
- App builds via xcodebuild with no missing-symbol or Metal-shader errors
- Plain dictation still copies raw transcript to clipboard — no regression
- build.sh at repo root documents the xcodebuild-only constraint with inline comment about default.metallib
</success_criteria>

<output>
After completion, create `.planning/phases/06-dependency-integration-and-build-gate/06-01-SUMMARY.md`
</output>
