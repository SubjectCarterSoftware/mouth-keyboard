---
phase: 05-long-dictation-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - Speech2Test/Activation/ActivationStore.swift
  - Speech2Test/App/AppDelegate.swift
  - Speech2Test/Audio/AudioBufferAccumulator.swift
  - Speech2Test/Audio/AudioLevelMonitor.swift
  - Speech2Test/Shell/StatusMenuView.swift
  - Speech2TestTests/ActivationStoreTests.swift
  - Speech2TestTests/AudioBufferAccumulatorTests.swift
  - Speech2TestTests/LongDictationBoundaryTests.swift
  - Speech2TestTests/TranscriptAssemblerTests.swift
  - Speech2TestTests/LongDictationFlowTests.swift
  - Speech2TestUITests/MenuBarShellSmokeTests.swift
  - Speech2Test.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - TRNS-03
  - TRNS-04
  - TRNS-05
user_setup:
  - Have the Whisper model available in the app bundle and a working microphone ready for at least one >30-second dictation run during the human verification checkpoint.

must_haves:
  truths:
    - "A >30-second dictation session can cross the threshold, seal multiple segments, and still produce one ordered final clipboard result."
    - "Continuous speech without a natural pause still preserves early audio by sealing on the soft cap."
    - "Partial-failure sessions keep successful text in the final clipboard result and surface a count-based warning outside the clipboard text."
    - "A reproducible test-only segment-failure injection path exists for UI and manual verification without changing normal user-facing behavior."
    - "Indicator-hidden mode still provides persistent long-session status and warning visibility through the menu."
    - "Short-session behavior remains intact after the long-dictation changes."
  artifacts:
    - path: "Speech2TestTests/LongDictationFlowTests.swift"
      provides: "Cross-segment regression coverage for threshold activation, queue settlement, ordered assembly, and partial failure"
      contains: "testLongDictation"
    - path: "Speech2TestTests/ActivationStoreTests.swift"
      provides: "Session-level safety coverage for clipboard barrier and stale-result suppression after long-session changes"
      contains: "testLongSession"
    - path: "Speech2TestUITests/MenuBarShellSmokeTests.swift"
      provides: "Menu smoke coverage for indicator-hidden long-session status and warning surfaces"
      contains: "statusMenu"
    - path: "Speech2Test/App/AppDelegate.swift"
      provides: "Test-only launch-argument plumbing for deterministic segment-failure injection during verification"
      contains: "ui-testing-long-session-fail-segment"
    - path: "Speech2Test/Activation/ActivationStore.swift"
      provides: "Any targeted production fixes needed to satisfy the reliability regression suite"
      contains: "longDictation"
  key_links:
    - from: "Speech2TestTests/LongDictationFlowTests.swift"
      to: "Speech2Test/Activation/ActivationStore.swift"
      via: "delayed and failing segment transcriber doubles exercise real queue-settlement paths"
      pattern: "finish|handleLongDictation"
    - from: "Speech2TestUITests/MenuBarShellSmokeTests.swift"
      to: "Speech2Test/Shell/StatusMenuView.swift"
      via: "indicator-hidden and warning identifiers prove the menu remains the persistent status surface"
      pattern: "statusMenu"
---

<objective>
Verify long-session reliability end to end, close regression gaps, and complete the manual long-dictation checkpoint.

Purpose: Phase 5 changes the core capture/transcription timing model. This final plan proves the system remains reliable under longer sessions, out-of-order / failed segment results, and the quieter menu-bar shell posture.

Output: long-session regression coverage, any targeted fixes required by that coverage, and a completed human verification checkpoint for real long dictation behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-long-dictation-reliability/05-CONTEXT.md
@.planning/phases/05-long-dictation-reliability/05-RESEARCH.md
@.planning/phases/05-long-dictation-reliability/05-VALIDATION.md
@.planning/phases/05-long-dictation-reliability/05-01-PLAN.md
@.planning/phases/05-long-dictation-reliability/05-01-SUMMARY.md
@.planning/phases/05-long-dictation-reliability/05-02-PLAN.md
@.planning/phases/05-long-dictation-reliability/05-02-SUMMARY.md
@Speech2Test/Activation/ActivationStore.swift
@Speech2Test/Shell/StatusMenuView.swift
</context>

<interfaces>
This plan verifies the full Phase 5 contract after Plans 01 and 02 land:

```swift
@MainActor
final class ActivationStore: ObservableObject {
    @Published private(set) var state: RecordingState
    @Published private(set) var longSessionStatus: LongSessionStatus?
    @Published private(set) var resultNotice: LongSessionResultNotice?
}
```

```swift
struct AssembledTranscript: Equatable {
    let text: String
    let failedSegmentCount: Int
    let successfulSegmentCount: Int
}
```

```swift
struct StatusMenuView: View {
    // receives lifecycle state plus long-session status / warning state
}
```

The executor should prefer fixing reliability gaps by tightening existing long-session seams, not by introducing a second verification-only code path.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the long-session regression matrix and close any targeted reliability gaps it reveals</name>
  <files>
    Speech2Test/Activation/ActivationStore.swift,
    Speech2Test/App/AppDelegate.swift,
    Speech2Test/Audio/AudioBufferAccumulator.swift,
    Speech2Test/Audio/AudioLevelMonitor.swift,
    Speech2Test/Shell/StatusMenuView.swift,
    Speech2TestTests/ActivationStoreTests.swift,
    Speech2TestTests/AudioBufferAccumulatorTests.swift,
    Speech2TestTests/LongDictationBoundaryTests.swift,
    Speech2TestTests/TranscriptAssemblerTests.swift,
    Speech2TestTests/LongDictationFlowTests.swift,
    Speech2TestUITests/MenuBarShellSmokeTests.swift,
    Speech2Test.xcodeproj/project.pbxproj
  </files>
  <action>
    Build the regression suite Phase 5 needs and make only the production fixes required to satisfy it.

    Implementation requirements:
    - Add `LongDictationFlowTests.swift` with mocked transcriber timing so the suite can prove:
      - multi-segment success after crossing the threshold
      - out-of-order completion still yields ordered final output
      - soft-cap sealing preserves early audio during continuous speech
      - partial failure still copies successful text and records a warning count
      - all-failure long sessions leave the clipboard untouched
      - short sessions below the threshold still use the existing non-segmented behavior
    - Add a reproducible test-only failure injection path for partial-failure verification if one does not already exist. Prefer a launch argument such as `-ui-testing-long-session-fail-segment <index>` that forces the chosen sealed segment to fail transcription under `-ui-testing` or another clearly non-production mode.
    - Expand the existing tests where needed so the validation matrix in `05-VALIDATION.md` is genuinely covered instead of nominally referenced.
    - Run the new regression suite and apply only targeted fixes in `ActivationStore`, `AudioBufferAccumulator`, `AudioLevelMonitor`, or `StatusMenuView` where failures expose real gaps. Do not re-architect the feature in this plan.
    - Keep UI automation focused on stable menu identifiers, the test-only partial-failure path, and indicator-hidden status/warning behavior. Do not add brittle animation assertions.
  </action>
  <verify>xcodebuild test -project Speech2Test.xcodeproj -scheme Speech2Test -destination 'platform=macOS'</verify>
  <done>
    - automated coverage exists for the long-session success, soft-cap, partial-failure, all-failure, and short-session-regression paths
    - any production fixes are narrow and directly justified by failing coverage
    - the full suite is green before the human checkpoint begins
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of long-session capture, soft-cap behavior, and persistent warning visibility</name>
  <files>N/A</files>
  <action>
    Verify the real-world behaviors automated tests cannot prove well enough on their own.

    Test 1 — Long dictation with natural pauses (TRNS-03, TRNS-04):
    1. Start recording in another app.
    2. Dictate for more than 30 seconds, including at least two clear pauses.
    3. Finish the session.
    4. Verify the final clipboard text contains the spoken content in order and does not visibly expose internal segment boundaries.

    Test 2 — Continuous speech soft-cap path (TRNS-03):
    1. Start recording.
    2. Speak continuously past the long-session threshold without offering a clear pause.
    3. Finish the session.
    4. Verify the app still returns earlier spoken content instead of losing pre-cap audio.

    Test 3 — Indicator-hidden persistent status (TRNS-05):
    1. Hide the recording indicator.
    2. Run a long-dictation session.
    3. Verify the menu remains the persistent status surface during finalization and, if the result is incomplete, shows the warning there.

    Test 4 — Partial-failure warning path (TRNS-05):
    1. Launch the app in the verification mode created in Task 1, for example with `-ui-testing-long-session-fail-segment 2`.
    2. Finish the long session.
    3. Verify the clipboard contains only successful text, and the UI warning is count-based rather than embedding failure markers in the pasted text.
  </action>
  <verify>Human tester completes the four scenarios above on a real macOS machine with a working microphone and model bundle.</verify>
  <done>
    Long dictation behaves reliably under real speech cadence, continuous-speech soft-cap conditions, indicator-hidden mode, and partial-failure warning visibility.
  </done>
</task>

</tasks>
