---
phase: 08-llm-rewrite-service
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - Speech2Text/Conversion/LLMRewriteService.swift
  - Speech2TextTests/LLMRewriteServiceIntegrationTests.swift
  - Speech2Text.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - GUARD-02

must_haves:
  truths:
    - "Opt-in integration coverage proves the real `rewrite(body:mode:)` path returns trimmed non-empty output for all six built-in modes."
    - "The same service instance reuses its loaded model container across multiple real rewrite calls instead of cold-loading the model every time."
    - "Integration tests are gated behind `ENABLE_LLM_INTEGRATION_TESTS=1`, so default local and CI feedback stays fast and deterministic."
    - "If real-model testing exposes truncation or parameter instability, the service is adjusted to preserve the throw-on-truncation contract rather than weakening the tests."
    - "Full-suite regression stays green after the rewrite-service coverage is added."
    - "Manual profiling confirms inference work remains off the main thread and that the second rewrite does not re-download or re-cold-load the model."
  artifacts:
    - path: "Speech2TextTests/LLMRewriteServiceIntegrationTests.swift"
      provides: "Opt-in real-model integration coverage for all six modes plus model-reuse validation"
      contains: "LLMRewriteServiceIntegrationTests"
    - path: "Speech2Text/Conversion/LLMRewriteService.swift"
      provides: "Any production refinements required by real-model testing without weakening the Phase 8 failure contract"
      contains: "rewrite(body: String, mode: ConvertMode)"
    - path: "Speech2Text.xcodeproj/project.pbxproj"
      provides: "Test-target membership for the integration suite"
      contains: "LLMRewriteServiceIntegrationTests"
  key_links:
    - from: "Speech2TextTests/LLMRewriteServiceIntegrationTests.swift"
      to: "Speech2Text/Conversion/LLMRewriteService.swift"
      via: "real-model integration tests call the same `rewrite(body:mode:)` API that Phase 9 will use"
      pattern: "rewrite(body:mode:)"
    - from: "Speech2TextTests/LLMRewriteServiceIntegrationTests.swift"
      to: "Speech2Text/Conversion/ConvertMode.swift"
      via: "the suite iterates all six built-in modes and uses their locked prompts via the service"
      pattern: "ConvertMode"
    - from: "Speech2Text.xcodeproj/project.pbxproj"
      to: "Speech2TextTests/LLMRewriteServiceIntegrationTests.swift"
      via: "PBXBuildFile + test-target membership"
      pattern: "LLMRewriteServiceIntegrationTests"
---

<objective>
Add opt-in real-model integration coverage, run the full regression gate, and complete the manual performance/cache-reuse verification for Phase 8.

Purpose: Plan 01 proves the service contract deterministically. This plan validates that the real MLX/Qwen path satisfies the roadmap success criteria on a developer machine without forcing heavyweight model downloads into every default test run.
Output: `Speech2TextTests/LLMRewriteServiceIntegrationTests.swift`, any small service refinements needed after real-model execution, full-suite verification, and a human verification checkpoint for off-main inference and load reuse.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-llm-rewrite-service/08-RESEARCH.md
@.planning/phases/08-llm-rewrite-service/08-VALIDATION.md
@.planning/phases/08-llm-rewrite-service/08-01-PLAN.md
@.planning/phases/08-llm-rewrite-service/08-01-SUMMARY.md
@Speech2Text/Conversion/LLMRewriteService.swift
@Speech2Text/Conversion/ConvertMode.swift
@Speech2TextTests/LLMRewriteServiceTests.swift
</context>

<interfaces>
Plan 01 establishes the service contract this plan is validating:

```swift
enum LLMRewriteError: LocalizedError {
    case modelLoadFailed
    case generationFailed
    case cancelled
    case outputTruncated
    case emptyOutput
}

protocol LLMRewriting: Sendable {
    func rewrite(body: String, mode: ConvertMode) async throws -> String
}

actor LLMRewriteService: LLMRewriting {
    static let shared = LLMRewriteService()
}
```

Opt-in integration tests must be skippable by default:

```swift
guard ProcessInfo.processInfo.environment["ENABLE_LLM_INTEGRATION_TESTS"] == "1" else {
    throw XCTSkip("Set ENABLE_LLM_INTEGRATION_TESTS=1 to run real-model rewrite tests.")
}
```

Phase 8 success criteria to validate here:

```text
1. rewrite(body:mode:) returns a non-empty rewritten string for each of the 6 modes
2. failures throw and return no partial output
3. main thread is not blocked during inference
4. the model is loaded once and reused across subsequent rewrite calls in the same session
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add opt-in real-model integration tests for all modes and model reuse</name>
  <files>
    Speech2TextTests/LLMRewriteServiceIntegrationTests.swift,
    Speech2Text/Conversion/LLMRewriteService.swift,
    Speech2Text.xcodeproj/project.pbxproj
  </files>
  <action>
    Create `Speech2TextTests/LLMRewriteServiceIntegrationTests.swift` and add it to the Speech2TextTests target. Adjust `LLMRewriteService.swift` only if real-model execution reveals a production issue or a small testability seam is still missing.

    Integration test requirements:
    - Gate the suite behind `ENABLE_LLM_INTEGRATION_TESTS=1`. If the variable is absent, skip cleanly with `XCTSkip` instead of failing.
    - Use the real `LLMRewriteService` production path, not a fake backend.
    - Add one integration test that exercises all six built-in modes with short representative inputs and asserts the returned rewrite is trimmed and non-empty for each mode. Prefer naming it `testRewritesAllBuiltInModes`.
    - Add one integration test that proves model reuse on the same service instance. Preferred approach: use the Plan 01 test seam to wrap the real load path with a counting proxy, then assert two real rewrite calls trigger one underlying load on that service instance. Prefer naming it `testRealRewriteReusesLoadedModelOnSecondCall`.
    - Do not assert exact wording for the rewritten output. These are semantic smoke tests, not golden-output tests.
    - If the real model consistently truncates a valid mode output, raise the service's token budget or other generation parameter in `LLMRewriteService.swift`. Do not relax the `.outputTruncated` contract or accept clipped output.

    After writing the suite, run the opt-in integration tests:

    ```bash
    ENABLE_LLM_INTEGRATION_TESTS=1 xcodebuild test \
      -project Speech2Text.xcodeproj \
      -scheme Speech2Text \
      -destination 'platform=macOS,arch=arm64' \
      -only-testing:Speech2TextTests/LLMRewriteServiceIntegrationTests
    ```
  </action>
  <verify>
    <automated>ENABLE_LLM_INTEGRATION_TESTS=1 xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -destination 'platform=macOS,arch=arm64' -only-testing:Speech2TextTests/LLMRewriteServiceIntegrationTests</automated>
  </verify>
  <done>
    - opt-in real-model integration tests exist and are wired into the test target
    - the suite proves non-empty rewrites for all six built-in modes
    - the suite proves one service instance reuses its loaded model across multiple calls
    - any real-model refinements preserve the throw-on-failure contract from Plan 01
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full Speech2Text test suite as the Phase 8 regression gate</name>
  <files>
    Speech2Text/Conversion/LLMRewriteService.swift,
    Speech2TextTests/LLMRewriteServiceTests.swift,
    Speech2TextTests/LLMRewriteServiceIntegrationTests.swift,
    Speech2Text.xcodeproj/project.pbxproj
  </files>
  <action>
    Run the full `Speech2TextTests` suite to confirm the rewrite-service additions do not break existing behavior.

    Command:

    ```bash
    xcodebuild test \
      -project Speech2Text.xcodeproj \
      -scheme Speech2Text \
      -destination 'platform=macOS,arch=arm64'
    ```

    Expected outcome:
    - default test runs stay green without needing `ENABLE_LLM_INTEGRATION_TESTS`
    - the new opt-in integration suite is skipped cleanly when the env var is absent
    - pre-existing tests such as `ActivationStoreTests`, `WhisperServiceTests`, and `IntentDetectorTests` remain green

    If a failure is caused by files owned by this plan, fix it and rerun. If an unrelated pre-existing failure appears, stop and document it instead of expanding scope silently.
  </action>
  <verify>
    <automated>xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -destination 'platform=macOS,arch=arm64'</automated>
  </verify>
  <done>
    - the full default suite passes
    - integration tests remain opt-in rather than slowing every default run
    - no Phase 8 regression leaks into existing transcription, activation, or intent-detection behavior
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification for off-main inference and no-repeat cold load behavior</name>
  <files>N/A</files>
  <action>
    Verify the two runtime behaviors automated tests cannot conclusively prove on their own: off-main inference and practical model reuse.
  </action>
  <what-built>
    `LLMRewriteService` now has deterministic unit coverage, opt-in real-model integration coverage, and a passing full default suite. The remaining gate is runtime observation of thread usage and first-load vs second-load behavior on a real macOS machine.
  </what-built>
  <how-to-verify>
    1. Optional first-load reset: remove the rewrite-model cache directory if you want to observe a cold start explicitly. Use the same Application Support path the service writes to (for example `~/Library/Application Support/Speech2Text/RewriteModel`).
    2. Run the real-model reuse test from Task 1 with `ENABLE_LLM_INTEGRATION_TESTS=1` and confirm the first rewrite performs the expected download/load work while the second rewrite in that same test run does not trigger a second underlying load.
    3. In Xcode, profile that same reuse test or an equivalent app-driven rewrite with Time Profiler. While generation is in progress, `com.apple.main-thread` should stay near idle and the MLX work should appear on background threads, not on the main thread.
    4. If the second rewrite still behaves like a cold load, inspect the service for accidental container re-creation, missing `loadTask` reuse, or an incorrect cache-path helper before approving the plan.
  </how-to-verify>
  <verify>Human tester completes the cache-reuse and Time Profiler checks above on a real macOS machine.</verify>
  <done>
    The real rewrite path stays off the main thread and practical reuse behavior matches the roadmap: first use loads the model, later rewrites reuse it instead of cold-loading again.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ENABLE_LLM_INTEGRATION_TESTS=1 xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -destination 'platform=macOS,arch=arm64' -only-testing:Speech2TextTests/LLMRewriteServiceIntegrationTests` succeeds
- [ ] `xcodebuild test -project Speech2Text.xcodeproj -scheme Speech2Text -destination 'platform=macOS,arch=arm64'` succeeds
- [ ] Human verification confirms off-main inference and no-repeat cold-load behavior on a real machine
</verification>

<success_criteria>
- real-model integration tests prove non-empty rewrites for all six built-in modes
- the same service instance reuses the loaded model across multiple rewrite calls
- the full default test suite remains green with integration coverage still opt-in
- runtime profiling confirms inference does not block the main thread
</success_criteria>

<output>
After completion, create `.planning/phases/08-llm-rewrite-service/08-02-SUMMARY.md`
</output>
