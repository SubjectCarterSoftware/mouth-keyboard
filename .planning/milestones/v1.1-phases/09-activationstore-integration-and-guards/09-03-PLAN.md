---
phase: 09-activationstore-integration-and-guards
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified: []
autonomous: false
requirements:
  - LLM-02
  - GUARD-01
  - UX-01

must_haves:
  truths:
    - "Pill shows distinct blue/animated indicator during .converting (not the white processing dots)"
    - "350-word body triggers orange pill with 'Input exceeds AI limit' and raw transcript in clipboard"
    - "LLM failure delivers raw transcript to clipboard silently with .success state visible"
    - "Plain dictation path is completely unchanged — no visual or behavioral regression"
    - "'Copy Last AI Converted Transcription' menu item is enabled after a conversion and disabled after plain dictation"
  artifacts:
    - path: "Speech2Text/Shell/RecordingPillView.swift"
      provides: "Visual rendering of all new states confirmed in running app"
    - path: "Speech2Text/Shell/StatusMenuView.swift"
      provides: "Menu item correctly enabled/disabled based on lastConvertedTranscription"
  key_links:
    - from: "Running app"
      to: "Human verifier"
      via: "Live dictation triggering each guard and UX path"
      pattern: "manual verification"
---

<objective>
Human-verify the three behaviors that cannot be covered by automated tests: the 350-word word-limit guard (GUARD-01), the LLM failure silent fallback, and the visual distinction of the .converting pill animation (UX-01).

Purpose: Automated tests cover the happy path and passthrough path. These three behaviors require real app execution to verify correctly — orange pill flash, silence on LLM failure, and perceptible animation difference.

Output: Phase 9 sign-off with manual verification confirmed.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-activationstore-integration-and-guards/09-01-SUMMARY.md
@.planning/phases/09-activationstore-integration-and-guards/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch app for manual verification</name>
  <files></files>
  <action>
    Build and run the app via Xcode or xcodebuild. Confirm it launches without crashing.

    Run: `xcodebuild build -scheme Speech2Text -destination "platform=macOS" 2>&1 | tail -20`

    If build succeeds, open the app in Xcode (or via the built product) and confirm the menu bar item appears.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Speech2Text -destination "platform=macOS" 2>&1 | grep -c "BUILD SUCCEEDED"</automated>
  </verify>
  <done>App builds successfully and launches. Menu bar item visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — .converting animation, 350-word guard, LLM fallback, passthrough, menu item</name>
  <what-built>
    Three Phase 9 behaviors wired across Plans 01 and 02:
    1. 350-word guard (GUARD-01): triggers before LLM call, orange pill alert, raw transcript to clipboard
    2. LLM failure silent fallback: raw transcript to clipboard with no error visible, .success state
    3. .converting pill animation (UX-01): visually distinct from the white-dot .processing animation
  </what-built>
  <how-to-verify>
    Run the app and test each behavior:

    **Test A — .converting animation (UX-01):**
    1. Dictate any trigger phrase, e.g. "convert to email I want to schedule a call"
    2. After finishing dictation (hotkey), observe the pill during LLM inference
    3. EXPECTED: Pill shows a blue pulsing animation (NOT the white dots from Whisper processing). If LLM is fast, you may need a longer input to observe the .converting state.
    4. PASS: Animation is perceptibly different from the white processing dots

    **Test B — 350-word guard (GUARD-01):**
    1. Dictate: "convert to email [then speak or type 351+ words of content]"
    2. Alternatively, if manual testing is difficult, verify by reading ActivationStore.swift to confirm the wordCount <= 350 guard fires on intent.strippedBody before the LLM call, then test with a real long dictation
    3. EXPECTED: Pill flashes orange with "Input exceeds AI limit" text; raw transcript lands in clipboard; pill auto-dismisses after ~2 seconds
    4. PASS: Orange pill appears; clipboard has the raw transcript (not a rewritten version); no LLM output visible

    **Test C — LLM failure fallback (GUARD-02 verification):**
    1. To simulate LLM failure: if possible, temporarily modify LLMRewriteService to throw immediately, OR dictate a trigger phrase and verify via logging that a thrown error lands in the fallback path
    2. If full LLM stack is unavailable (no model downloaded), verify by dictating a trigger phrase and observing the result
    3. EXPECTED: Raw transcript copied to clipboard silently; pill shows "Copied" (not "Converted"); no error visible to user
    4. PASS: User sees a successful-looking result with the raw transcript — no indication conversion was attempted and failed

    **Test D — Passthrough unchanged (LLM-02 regression):**
    1. Dictate a plain message with no trigger phrase, e.g. "This is just a regular message"
    2. EXPECTED: Raw transcript copied to clipboard; pill shows "Copied"; no conversion attempted; lastTranscription set but lastConvertedTranscription remains nil from previous session (or nil if first run)
    3. PASS: Exact same behavior as before Phase 9

    **Test E — "Copy Last AI Converted Transcription" menu item:**
    1. After a successful conversion (Test A), open the menu bar menu
    2. EXPECTED: "Copy Last AI Converted Transcription" item is enabled and copies the rewritten text when clicked
    3. After a plain dictation (Test D), open the menu again
    4. EXPECTED: Item is disabled (grayed out) since lastConvertedTranscription was not updated
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work correctly, or describe which tests failed and what was observed instead</resume-signal>
  <action>Human runs the live app and manually verifies all 5 behaviors (A-E) described in how-to-verify. No code changes expected. If issues are found, report them for a gap-closure plan.</action>
  <verify>
    <automated>MISSING — manual-only; automated tests for this plan are satisfied by Plan 02 Task 3 full suite run</automated>
  </verify>
  <done>Human types "approved" after verifying all 5 behaviors pass in the live app.</done>
  <files>N/A — verification plan, no files modified</files>
</task>

</tasks>

<verification>
Phase 9 is complete when:
1. Full automated test suite green (confirmed in Plan 02)
2. Human verified: .converting animation is visually distinct from .processing
3. Human verified: 350-word guard fires with orange pill and raw transcript to clipboard
4. Human verified: LLM failure fallback is completely silent
5. Human verified: Passthrough path unchanged
6. Human verified: "Copy Last AI Converted Transcription" menu item works
</verification>

<success_criteria>
- Human sign-off on all 5 manual verification tests (A through E)
- No visual regressions on existing pill states (.recording, .processing, .success, .failure)
- Menu item correctly enabled/disabled based on lastConvertedTranscription
- Phase 9 roadmap success criteria all confirmed:
  1. Trigger-phrase dictation → LLM-rewritten text in clipboard
  2. Plain dictation → raw transcript unchanged
  3. 350-word body → orange pill alert + raw transcript
  4. LLM failure → raw transcript silently, no partial output
  5. Pill shows distinct loading indicator during LLM conversion
</success_criteria>

<output>
After completion, create `.planning/phases/09-activationstore-integration-and-guards/09-03-SUMMARY.md`
</output>
